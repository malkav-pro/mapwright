---
phase: 01-connected-imported-terrain
plan: 06
type: execute
wave: 4
depends_on: [01-02, 01-04, 01-05]
files_modified:
  - .planning/phases/01-connected-imported-terrain/01-Q3-DECISION.md
  - .planning/phases/01-connected-imported-terrain/01-Q5-DECISION.md
  - Scripts/Rendering/ConnectedTerrainGraph.cs
  - Scripts/Rendering/RenderResourceLedger.cs
  - Shaders/terrain_coverage.glsl
  - Shaders/terrain_coast.glsl
  - tests/Mapwright.ContractTests/RenderContracts.cs
requirements: [DOC-03, MASK-01, WATR-01, TERR-02, REND-01, REND-02, REND-03, REND-05]
autonomous: true
estimate: {tokens: 72000, raw_tokens: 72000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "One immutable revision/render graph paints Background, Foreground texture, Foreground coverage, then river subtraction, with document-anchored sampling, bounded tiles/mips and revision-aware invalidation."
    - "sRGB inputs convert to linear premultiplied composition and correct straight-alpha output; coverage/distance remain non-colour data and originals keep their identity (D-22)."
    - "Tiled and whole-image small references differ by at most one 8-bit channel; tile boundaries, sequential supports and asymmetric shadows are tested without a boundary-correlated seam."
    - "A zero-content map renders the declared transparent or project-colour underlay consistently in viewport/export (EDGE REND-01:empty)."
    - {statement: "Imported byte encodings and decoded colour profiles have one declared normalization path and retain original hash identity (EDGE REND-01:encoding)", verification: backstop}
    - "Resource admission observes editor GPU ≤min(25% VRAM,4 GiB) with headroom, decoded CPU cache ≤512 MiB, export buffers ≤512 MiB, process RAM ≤25% system RAM and history acceleration ≤4 GiB."
    - {statement: "Coast/soft-mask tile boundaries at exact threshold and one increment past retain seam tolerance (EDGE REND-03:boundary)", verification: backstop}
    - {statement: "Contour distance at active output scale uses a declared 0.5 source threshold and deterministic raster rounding when a distance field is used (EDGE REND-03:precision)", verification: backstop}
    - {statement: "Tile, halo and cache allocations at exact resource caps and one unit over fit or reject before allocation (EDGE REND-05:boundary)", verification: backstop}
    - {statement: "Hardware percentages, byte/GiB conversions and band rounding use checked arithmetic (EDGE REND-05:precision)", verification: backstop}
  assumptions:
    - "FLAGGED UNRESOLVED REND-02 unclassified probe: independently inspect colour/soft-alpha fixtures at import, viewport and PNG boundaries."
  prohibitions:
    - {statement: "The preview's baked coastline must not be claimed removable by native soft-mask editing", status: flagged-unverified}
  artifacts:
    - {path: Scripts/Rendering/RenderResourceLedger.cs, provides: "Hardware limits and cache/buffer accounting"}
    - {path: tests/Mapwright.ContractTests/RenderContracts.cs, provides: "Colour, seam, coastline branch and budget oracles"}
  key_links:
    - {from: Scripts/Rendering/ConnectedTerrainGraph.cs, to: src/Mapwright.Domain/MapModel.cs, via: "Revision-keyed fixed-role composition and invalidation"}
    - {from: Scripts/Rendering/ConnectedTerrainGraph.cs, to: Scripts/Rendering/RenderResourceLedger.cs, via: "Admission before tile/halo allocation"}
---
<objective>
Expand the shared revision renderer to correct colour/alpha, seamless bounded tiles, conditional fixed coast treatment and measured resource admission.
Purpose: viewport and export must use the same graph with no hidden dependence on GPU preview state.
Output: connected graph, resource ledger, reference fixtures and recorded coast branch.
</objective>
<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-connected-imported-terrain/01-CONTEXT.md
@.planning/phases/01-connected-imported-terrain/01-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-UI-SPEC.md
@.planning/phases/01-connected-imported-terrain/01-04-SUMMARY.md
@Shaders/terrain_sdf_color.glsl
@Scripts/Rendering/GpuJfaTerrainRenderer.cs
@Scripts/Rendering/GpuSeamProbe.cs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Render native texture, soft land and river with reference colour</name>
  <files>Scripts/Rendering/ConnectedTerrainGraph.cs, Shaders/terrain_coverage.glsl, tests/Mapwright.ContractTests/RenderContracts.cs, .planning/phases/01-connected-imported-terrain/01-Q3-DECISION.md</files>
  <read_first>Scripts/Rendering/ConnectedTerrainGraph.cs; Shaders/terrain_coverage.glsl; Shaders/terrain_sdf_color.glsl; Scripts/Rendering/GpuSeamProbe.cs; src/Mapwright.Domain/MapModel.cs; src/Mapwright.Domain/Commands.cs; .planning/REQUIREMENTS.md</read_first>
  <behavior>Tests compare colour/soft alpha for transparent, hard and soft edges; texture anchoring at tile boundaries/zoom/export scales; river after mask painting; frozen revision identity; whole-image/tiled reference with ≤1 channel difference, sequential-support and asymmetric-shadow fixtures.</behavior>
  <decision_gate id="Q3-underlay" type="evidence" before="empty-background-composition">Before composing empty Background pixels, inspect the real source preview, transparent/empty fixtures, export alpha requirements and Q3 solo truth table from plan 03. Compare transparent underlay and project-colour underlay in viewport and straight-alpha PNG; select one explicit project semantic and record sample pixels, user-visible interpretation, and matching viewport/export oracle in 01-Q3-DECISION.md. If neither can be reconciled with source preservation and the shared graph, halt composition and report the failed fixture. Do not infer an underlay from a mockup background colour.</decision_gate>
  <action>Make ConnectedTerrainGraph evaluate the committed snapshot only: Background colour, Foreground texture, base coverage, ordered Land strokes, river subtraction and final composite; future object/path/text consumer slots remain above terrain but do not introduce Phase 2/3 UI (D-07/D-08). Use explicit sRGB input decoding, linear-premultiplied math and correct output conversion; treat coverage/distance as scalar data and keep original image hashes. Choose and document the under-Background appearance, then apply it consistently. Use document coordinates across tile boundaries, revision/graph-input tile keys, conservative forward invalidation and backward dependency bounds. Keep transient gesture preview separate. Build a CPU whole-image oracle for small fixtures and assert ≤1-channel tiled/reference difference. Do not reuse the current merged-mask shore/ring shader as evidence of coast-only correctness.</action>
  <verify><automated>$p='.planning/phases/01-connected-imported-terrain/01-Q3-DECISION.md'; if (!(Test-Path $p) -or !(Select-String -Path $p -Pattern '^Underlay Decision: .+' -Quiet) -or !(Select-String -Path $p -Pattern '^Underlay Evidence: .+' -Quiet)) { exit 1 }; ./Scripts/run-contract-tests.ps1</automated><fails_when>Q3 underlay choice and matching pixel oracle are absent, a colour or alpha reference differs beyond one channel, a river channel closes, tile seams correlate with tile borders, or the graph reads a newer mutable revision.</fails_when></verify>
  <acceptance_criteria>Shared evaluator produces stable viewport/export pixels from a frozen revision; soft edges and document-anchored texture survive tiled evaluation.</acceptance_criteria>
  <done>Revision rendering is colour-correct and seam-checked independently of coastline styling.</done>
</task>
<task type="auto" tdd="true">
  <name>Choose verified fixed outer-coast branch and enforce resource envelopes</name>
  <files>Scripts/Rendering/ConnectedTerrainGraph.cs, Scripts/Rendering/RenderResourceLedger.cs, Shaders/terrain_coast.glsl, tests/Mapwright.ContractTests/RenderContracts.cs, .planning/phases/01-connected-imported-terrain/01-Q5-DECISION.md</files>
  <read_first>Scripts/Rendering/ConnectedTerrainGraph.cs; Scripts/Rendering/GpuJfaTerrainRenderer.cs; Shaders/terrain_coast.glsl; Shaders/terrain_sdf_color.glsl; tests/Mapwright.ContractTests/RenderContracts.cs; .planning/phases/01-connected-imported-terrain/01-CONTEXT.md; .planning/phases/01-connected-imported-terrain/01-RESEARCH.md</read_first>
  <behavior>Fixtures cover open outer coast, inland river bank, river mouth, lake-like cut, soft imported coverage and tile crossing; if fixed style is used, contour reference error ≤0.5 output pixel and no inland bank decoration. Budget tests exercise exact cap and one unit over for GPU, CPU, band, halo and history acceleration.</behavior>
  <decision_gate id="Q5" type="evidence" before="coast-style-implementation">Before adding generated coast styling, run bounded real-import and synthetic fixtures for open sea, inland river bank, river mouth, lake-like cut, soft coverage and tile crossings at target scales. Record source fixture hash, bank identity comparison, ≤0.5-output-pixel contour error if distance is used, resource measurements, failures and selected branch in 01-Q5-DECISION.md. Choose one fixed outer-coast treatment only if all D-19/D-21/D-22 checks pass; otherwise choose D-20's authorized unstyled generated-edge branch while keeping soft mask and bank softness. An unavailable or inconclusive fixture cannot count as proof of styling and takes the unstyled branch with the reason recorded.</decision_gate>
  <action>Run a bounded feasibility fixture on real imported coverage and the listed coast/river/mouth/tile cases. Prefer one fixed outer-coast treatment only if bank identity and ≤0.5-output-pixel distance error against the declared 0.5 source contour are proven within the resource budget (D-19/D-21/D-22). Otherwise implement D-20's authorized unstyled generated-edge branch; record the branch and evidence/reason in SUMMARY and mark unused distance/style checks not applicable, never passed. Do not introduce manual classification, extra mask layers, decorative fades/rings/isolines or coast controls. Keep soft mask and bank softness in either branch. Query reported VRAM/system RAM at startup and make a resource ledger account for engine/compositor headroom, GPU 25%/4 GiB cap, decoded CPU 512 MiB, export buffers 512 MiB, process 25% RAM and 4 GiB history acceleration. Admit smaller bands/concurrency or a clear unsupported/budget outcome before allocation. Device absence yields an inspection/recovery path rather than a CPU editor. Register RenderReference in ConnectedTerrainGraph.cs through the Plan 01 connected case registry; execute reference, branch and budget assertions, return a nonzero assertion count and propagate failures. Assert -ListCases contains RenderReference before invocation.</action>
  <verify><automated>$p='.planning/phases/01-connected-imported-terrain/01-Q5-DECISION.md'; if (!(Test-Path $p) -or !(Select-String -Path $p -Pattern '^Decision: .+' -Quiet) -or !(Select-String -Path $p -Pattern '^Evidence: .+' -Quiet)) { exit 1 }; ./Scripts/run-connected-phase1.ps1 -Case RenderReference</automated><fails_when>Q5 lacks a measured branch record, style decorates an inland bank/mouth, an active distance field exceeds contour tolerance, an unused distance check is reported passed, a budget is exceeded, or tiled/reference pixels differ by more than one channel.</fails_when></verify>
  <acceptance_criteria>Selected style branch is documented with fixture evidence; colour/seam gates pass regardless of branch; allocations remain inside configured and measured bounds.</acceptance_criteria>
  <done>Renderer chooses one honest coast branch and enforces resource admission without weakening rendering correctness.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Imported raster/shader inputs → GPU | Hostile dimensions and profiles could cause huge allocations or invalid reads. |
| GPU/cache → displayed/exported pixels | Incompatible or stale tiles could misrepresent the authoritative revision. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-19 | Denial of service | tile/halo allocation | high | mitigate | Startup hardware query, checked arithmetic and admission ledger before allocation. |
| T-01-20 | Tampering | revision tile cache | medium | mitigate | Key tiles by revision/inputs/version and verify invalidation/rebuild against reference. |
| T-01-21 | Information disclosure | stale GPU tile | medium | mitigate | Clear/rekey recycled tiles and never show pixels from another revision/project. |
</threat_model>
<verification>Run colour/reference contracts and RenderReference case on recorded hardware. Preserve numeric results, branch rationale and N/A status for unused distance checks.</verification>
<success_criteria>Viewport and export graph agree to one channel or less on references; selected coast branch is honest; all admission budgets have enforceable limits.</success_criteria>
## Artifacts this phase produces
- ConnectedTerrainGraph full terrain/river evaluator and revision-aware tile cache.
- RenderResourceLedger with hardware query, headroom and admission results.
- RenderContracts reference corpus and conditional coast decision record.
<output>Create .planning/phases/01-connected-imported-terrain/01-06-SUMMARY.md when done.</output>
