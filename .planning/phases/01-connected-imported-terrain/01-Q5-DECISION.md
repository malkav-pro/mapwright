# Q5 Coastline Branch Decision

Decision: Use D-20's authorized `UnstyledGeneratedEdge` branch; keep source soft coverage and river bank softness, and remove generated shoreline/ring/isolines from product colour.

Evidence: The real import fixture SHA-256 is `67b69858634b1a2eee8082efd80043e915dcebb36e29c8dc66e42c6406e54fab`; its `layer-fg/mask` contains intermediate alpha values, but the imported coverage and current generated coverage do not carry a trustworthy boundary identity that distinguishes open outer coast from inland river bank, river mouth, or lake-like cut. Bounded synthetic fixtures for those four identities plus soft coverage and a 16-pixel tile crossing retain only the base terrain colour and compare whole/tiled byte-identically (`max_channel_difference=0`). The previous merged-mask shader decorated every threshold boundary, so D-19/D-21 outer-coast identity is inconclusive and cannot authorize styling. The style path and its <=0.5-output-pixel contour-distance check are therefore **N/A (inactive), not passed**. On the verification machine Godot reported `33,443,094,528` bytes physical RAM, a process limit of `8,360,773,632` bytes, and `0` bytes device memory/headroom in this release build; GPU work therefore receives the explicit `InspectionOnly`/recovery outcome rather than an invented capacity or CPU-editor fallback. CPU reference rendering remains admitted against its measured process cap. Synthetic `16 GiB` VRAM / `32 GiB` RAM ledger checks prove the formula (`min(25%, 4 GiB) - 512 MiB = 3,758,096,384` GPU bytes), exact caps and one byte/row over for GPU, `512 MiB` decoded CPU, `512 MiB` export band plus halo, `25%` process RAM, and `4 GiB` history acceleration. The registered `RenderReference` case passed 19 assertions on that recorded hardware.
