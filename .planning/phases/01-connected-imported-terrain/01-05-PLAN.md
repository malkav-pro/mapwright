---
phase: 01-connected-imported-terrain
plan: 05
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - .planning/phases/01-connected-imported-terrain/01-Q4-DECISION.md
  - src/Mapwright.Application/Ports.cs
  - src/Mapwright.Application/EditSession.cs
  - src/Mapwright.Application/HistorySession.cs
  - src/Mapwright.Infrastructure/SqliteProjectRepository.cs
  - tests/Mapwright.ContractTests/HistoryContracts.cs
requirements: [DOC-02, HIST-01, HIST-02, HIST-04, DURA-01]
autonomous: true
estimate: {tokens: 67000, raw_tokens: 67000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Completed gestures and property changes are one durable history step; cursor and redo branch survive restart (D-24/D-27)."
    - "Recent compatible before/after deltas restore up to 16 resident tiles within the stated p95 gate; older/evicted steps reconstruct with real progress and cancellation (D-25)."
    - "A save drains all queued commands and reports Saved only after durable acknowledgement; repeated Save with empty queue is a no-op (D-27)."
    - "Adjacent or equal revision/cursor transitions commit in one ordered transaction (EDGE DURA-01:adjacency)."
    - "An empty command batch does not advance the durable revision (EDGE DURA-01:empty)."
    - "Equal-time commands retain committed sequence order (EDGE DURA-01:ordering)."
  assumptions:
    - "FLAGGED UNRESOLVED HIST-01 unclassified probe: audit cursor and redo state after restart at the branch point."
    - "FLAGGED UNRESOLVED HIST-02 unclassified probe: audit resident/evicted reconstruction and cancellation under memory pressure."
    - "FLAGGED UNRESOLVED HIST-04 unclassified probe: audit renderer/algorithm compatibility and source retention after cache invalidation."
  artifacts:
    - {path: src/Mapwright.Application/HistorySession.cs, provides: "Durable cursor navigation and cancellable reconstruction"}
    - {path: tests/Mapwright.ContractTests/HistoryContracts.cs, provides: "Cursor, redo, save and delta contracts"}
  key_links:
    - {from: src/Mapwright.Application/HistorySession.cs, to: src/Mapwright.Infrastructure/SqliteProjectRepository.cs, via: "Durably commit cursor before publishing reconstructed snapshot"}
    - {from: src/Mapwright.Application/EditSession.cs, to: src/Mapwright.Application/Ports.cs, via: "Ordered commit queue and Save drain"}
---
<objective>
Expand the connected session into durable undo/redo, honest save/autosave, resident deltas and cancellable older replay.
Purpose: history is authoritative across restart while acceleration remains disposable and bounded.
Output: history session, repository cursor/checkpoints and automated contracts.
</objective>
<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-connected-imported-terrain/01-CONTEXT.md
@.planning/phases/01-connected-imported-terrain/01-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-VALIDATION.md
@.planning/phases/01-connected-imported-terrain/01-03-SUMMARY.md
@Scripts/Core/EditingDocument.cs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Persist cursor, redo branching and save-drain state</name>
  <files>src/Mapwright.Application/Ports.cs, src/Mapwright.Application/EditSession.cs, src/Mapwright.Application/HistorySession.cs, src/Mapwright.Infrastructure/SqliteProjectRepository.cs, tests/Mapwright.ContractTests/HistoryContracts.cs</files>
  <read_first>src/Mapwright.Application/Ports.cs; src/Mapwright.Application/EditSession.cs; src/Mapwright.Infrastructure/SqliteProjectRepository.cs; Scripts/Core/EditingDocument.cs; tests/Mapwright.ContractTests/Program.cs; .planning/phases/01-connected-imported-terrain/01-CONTEXT.md</read_first>
  <behavior>Tests cover one gesture/step, undo and redo across reopen, new edit after undo dropping redo transactionally, stale concurrent cursor requests, repeated Save, failure before commit, and UI-visible queue state following the durable event order.</behavior>
  <action>Extend IProjectRepository with cursor transition and command-list operations while retaining the EditSession commit→publish→invalidate→acknowledge order. Use one SQLite WAL FULL transaction for command, revision/materialized state, cursor and reference changes; new edits after undo transactionally invalidate the redo branch (HIST-01, D-27). Persist history cursor separately from latest revision and choose a stable command sequence tie break. Add an ordered commit queue and SaveAsync that awaits its drain; choose an autosave cadence and record it in the plan summary, without falsely treating a transient preview as unsaved authoritative data. Expose queued/saving/saved/failure facts to UI. Parameterize all SQL. Keep snapshots versioned for import/tracer migration. Retain the tracer's existing snapshot format only as input for the Q4 measurement gate in Task 2; do not adopt a checkpoint cadence or replay policy before that gate records its evidence.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>Undo cursor resets on restart, redo survives a new edit, Save reports completion before queued commits, or a failed transaction publishes a new snapshot.</fails_when></verify>
  <acceptance_criteria>History cursor and branch are durable; Save drains pending commands; no edit is acknowledged before blobs and transaction commit.</acceptance_criteria>
  <done>Users can undo/redo and save with truthful durable state after restart.</done>
</task>
<task type="auto" tdd="true">
  <name>Restore resident edits quickly and older steps coherently</name>
  <files>src/Mapwright.Application/HistorySession.cs, src/Mapwright.Infrastructure/SqliteProjectRepository.cs, tests/Mapwright.ContractTests/HistoryContracts.cs, .planning/phases/01-connected-imported-terrain/01-Q4-DECISION.md</files>
  <read_first>src/Mapwright.Application/HistorySession.cs; src/Mapwright.Infrastructure/SqliteProjectRepository.cs; src/Mapwright.Domain/MapModel.cs; tests/Mapwright.ContractTests/HistoryContracts.cs; .planning/REQUIREMENTS.md</read_first>
  <behavior>Tests cover compatible compressed before/after tile deltas, more than 16 tiles, evicted replay, cancelled reconstruction leaving the old cursor/view intact, replay after deleting all acceleration caches, and renderer/recipe version mismatch rebuilding without changing source commands.</behavior>
  <decision_gate id="Q4" type="evidence" before="checkpoint-replay-policy">Before implementing checkpoint cadence and compatibility handling, measure real-map command replay time, snapshot bytes, cache growth, evicted reconstruction and cancellation across candidate checkpoint intervals. In 01-Q4-DECISION.md record fixture hash, renderer/recipe/source compatibility identifiers, measured table, selected cadence and reason against the 4 GiB acceleration budget and history latency/cancellation contract. Options are retained full snapshots only if measured within all budgets, or periodic checkpoints plus complete command replay; unsupported version changes rebuild disposable caches and preserve original commands. If neither meets the gates, halt policy selection with measured failures instead of guessing a cadence.</decision_gate>
  <action>Store or derive bounded compressed before/after resident deltas keyed by immutable revision/recipe compatibility; treat them as acceleration only. Reconstruct older history from versioned checkpoints plus retained commands and immutable texture/source blobs with progress in real units. Make cancellation atomic from the user's perspective: do not publish a partial snapshot or cursor; pan/zoom may continue while edits are temporarily blocked (D-25). Measure checkpoint density against the 4 GiB acceleration budget without deleting authoritative history. Provide versioned renderer/algorithm compatibility metadata; incompatible cache rebuilds, and any output-changing upgrade warns and preserves original source/recipe (HIST-04).</action>
  <verify><automated>$p='.planning/phases/01-connected-imported-terrain/01-Q4-DECISION.md'; if (!(Test-Path $p) -or !(Select-String -Path $p -Pattern '^Decision: .+' -Quiet) -or !(Select-String -Path $p -Pattern '^Evidence: .+' -Quiet)) { exit 1 }; ./Scripts/run-contract-tests.ps1</automated><fails_when>Q4 measurement/cadence record is absent, evicted undo changes the wrong revision, cancellation leaves half-applied state, cache deletion loses history, or compatibility mismatch reuses stale pixels.</fails_when></verify>
  <acceptance_criteria>Resident and evicted history reconstruct the same document; cancellation keeps the old cursor; recent ≤16-tile undo is measurable for its p95 gate.</acceptance_criteria>
  <done>Both fast and reconstructed history paths preserve coherent, durable revisions.</done>
</task>
<task type="auto">
  <name>Expose revision-labelled history and queue facts for the shell</name>
  <files>src/Mapwright.Application/Ports.cs, src/Mapwright.Application/HistorySession.cs, tests/Mapwright.ContractTests/HistoryContracts.cs</files>
  <read_first>src/Mapwright.Application/Ports.cs; src/Mapwright.Application/HistorySession.cs; tests/Mapwright.ContractTests/HistoryContracts.cs; .planning/phases/01-connected-imported-terrain/01-UI-SPEC.md</read_first>
  <action>Return stable page/window queries for a virtualized action list with label, revision, timestamp, current cursor and measured tile metadata. Expose next Undo/Redo action/reason, save queue count and history rebuild progress/cancel token without owning Godot UI. D-23 places visible buttons and a collapsible history panel in plan 08; this task provides their authoritative data. Emit structured revision sequence and timing events so later acceptance can correlate history action to visible revision.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>History paging skips/duplicates equal-time commands, disabled-action reason is wrong, or queue state reports Saved before drain.</fails_when></verify>
  <acceptance_criteria>UI can page the complete durable action list and display exact revision, cursor, next action, queue and reconstruction status.</acceptance_criteria>
  <done>History and save information is available without deriving it from rendered pixels.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Command queue → SQLite | Untrusted UI actions become durable ordered history. |
| SQLite/cache → reconstructed snapshot | Corrupt or incompatible acceleration could misrepresent authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15 | Tampering | cursor/revision transaction | high | mitigate | Base-revision compare and atomic SQLite cursor/command/reference update. |
| T-01-16 | Denial of service | history replay | medium | mitigate | Bounded batches, real progress and cancellation retaining old snapshot. |
| T-01-17 | Tampering | delta cache | medium | mitigate | Version/hash validation and rebuild from authoritative commands. |
| T-01-18 | Repudiation | save chip | low | accept | Saved state is local and derived from durable commit time/queue; no remote audit identity is required. |
</threat_model>
<verification>Run contracts after every task; record resident undo distribution and evicted/cancel tests. The full p95 hardware gate occurs in plan 09.</verification>
<success_criteria>History/cursor survive restart, redo branching is transactional, and neither save nor undo misstates durable/visible revision.</success_criteria>
## Artifacts this phase produces
- HistorySession, repository cursor/checkpoint/delta tables and application port methods.
- SaveAsync and queue/status events.
- HistoryContracts for restart, branch, cache eviction and cancellation.
<output>Create .planning/phases/01-connected-imported-terrain/01-05-SUMMARY.md when done.</output>
