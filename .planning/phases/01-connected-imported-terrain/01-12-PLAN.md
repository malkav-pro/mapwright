---
phase: 01-connected-imported-terrain
plan: 12
type: execute
gap_closure: true
wave: 10
depends_on: [01-11]
files_modified:
  - Scripts/Rendering/ConnectedTerrainGraph.cs
  - Scripts/App/MapCanvas.cs
  - Scripts/Acceptance/ConnectedAcceptance.cs
  - tests/Mapwright.ContractTests/RenderContracts.cs
requirements: [TERR-02, MASK-01, WATR-01, REND-01, REND-02, REND-03, REND-04, REND-05]
autonomous: true
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "A small paint or river edit redraws only tiles intersecting its conservative document-space effect bounds; initial/open/visibility changes may redraw the whole viewport."
    - "The acceptance presentation path uses the same connected canvas update as the editor and no per-edit PNG encode/decode roundtrip."
    - "Dirty-tile output matches full RenderReference within one channel across tile edges for texture, soft coverage and river subtraction."
    - "Texture upload, cache and GPU allocations remain within REND-05 budgets; no acknowledgement bypasses durable storage."
  artifacts:
    - {path: Scripts/Rendering/ConnectedTerrainGraph.cs, provides: "Decoded-source cache and bounded region evaluation"}
    - {path: Scripts/App/MapCanvas.cs, provides: "Revision-tagged dirty-tile presentation"}
---
<objective>Replace full-frame per-edit reference/Png work with a bounded revision-correct connected viewport path.</objective>
<tasks>
<task type="auto">
  <name>Implement bounded region invalidation and renderer cache</name>
  <files>Scripts/Rendering/ConnectedTerrainGraph.cs, tests/Mapwright.ContractTests/RenderContracts.cs</files>
  <read_first>Scripts/Rendering/ConnectedTerrainGraph.cs; src/Mapwright.Domain/Geometry.cs; src/Mapwright.Domain/Commands.cs; tests/Mapwright.ContractTests/RenderContracts.cs; .planning/phases/01-connected-imported-terrain/01-11-SUMMARY.md</read_first>
  <action>Cache verified decoded source rasters by immutable source blob hash with explicit ledger admission. Add a bounded `EvaluateRegion` entry using existing RenderReference region arguments and a conservative affected-bounds calculation for AddResolvedTextureStroke, AddLandStroke, MoveRiverPoint and width changes; fall back to full invalidation when bounds cannot be proven. Use 256×256 output tiles and invalidate old plus new river bounds on moves. Do not use prior output as authoritative source data.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>Nonzero exit, zero contracts discovered, or region/full-frame colour and seam assertions differ by more than one channel.</fails_when></verify>
  <acceptance_criteria>Changed-region output is pixel-equivalent to full reference for texture, Land and river fixtures, including edges and old river position.</acceptance_criteria>
</task>
<task type="auto">
  <name>Present dirty tiles through the real canvas</name>
  <files>Scripts/App/MapCanvas.cs, Scripts/Acceptance/ConnectedAcceptance.cs</files>
  <read_first>Scripts/App/MapCanvas.cs; Scripts/Acceptance/ConnectedAcceptance.cs; Scripts/Rendering/ConnectedTerrainGraph.cs; .planning/phases/01-connected-imported-terrain/01-11-SUMMARY.md</read_first>
  <action>Keep revision-tagged 256×256 tile textures in MapCanvas, render/upload only dirty tiles after durable acknowledgement, and draw them at their document positions in `_Draw`. Add an initial full tile population and explicit cache discard on project/source change. Route the hardware recorder through this same canvas update and retain matching revision/generation FramePostDraw correlation. Remove the recorder's per-update PNG encoding path without changing export's frozen reference semantics.</action>
  <verify><automated>./Scripts/run-phase1-acceptance.ps1 -SelfTest</automated><fails_when>Nonzero exit, missing draw-generation correlation, or the recorder still encodes a PNG for each edit.</fails_when></verify>
  <acceptance_criteria>Small edits update a bounded tile set and are only recorded presented after the matching `_Draw`; a short Vulkan diagnostic shows measured stage improvement without claiming full acceptance.</acceptance_criteria>
</task>
<verification>Run RenderReference, Tracer, Gestures and short Vulkan diagnostics, then compare tile output against full reference.</verification>
<success_criteria>The production viewport and acceptance recorder share a bounded, revision-correct update path.</success_criteria>
## Artifacts this phase produces
- `ConnectedTerrainGraph.EvaluateRegion`, conservative dirty-region calculation, revision-tagged tile texture cache and corresponding render contracts.
<output>Create 01-12-SUMMARY.md with measured before/after and remaining thresholds.</output>
