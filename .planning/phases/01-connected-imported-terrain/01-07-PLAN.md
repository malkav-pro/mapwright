---
phase: 01-connected-imported-terrain
plan: 07
type: execute
wave: 5
depends_on: [01-05, 01-06]
files_modified:
  - Scripts/Export/DocumentPngExport.cs
  - Scripts/Export/StreamingPngWriter.cs
  - Scripts/Export/PngValidator.cs
  - src/Mapwright.Infrastructure/SqliteProjectRepository.cs
  - Tools/StorageCrashWorker/Program.cs
  - tests/Mapwright.ContractTests/ExportRecoveryContracts.cs
requirements: [DOC-02, DOC-03, HIST-04, EXPT-01, DURA-01, DURA-02, REND-01, REND-03, REND-05]
autonomous: true
estimate: {tokens: 69000, raw_tokens: 69000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "A frozen revision with pinned blobs exports through the same graph while later edits continue; output is validated and atomically published only after flush, cancellation leaves the prior destination intact (D-27)."
    - "After kill/device loss during edit or export, a fresh process opens every acknowledged command and cursor without missing blob references or partial destination; only unacknowledged gesture state may be absent (D-26/D-27)."
    - "Adjacent commit and export-publication failure points recover either old or complete new state, with no intermediate published image (EDGE DURA-02:adjacency)."
    - "Recovery of an empty/no-command project is coherent (EDGE DURA-02:empty)."
    - "Repeated recovery produces the same acknowledged revision and cursor (EDGE DURA-02:idempotency)."
    - "Concurrent edit/export readers retain their pinned revision (EDGE DURA-02:concurrency)."
    - {statement: "Equal-timestamp commands and export jobs retain stable sequence/revision ordering after crash (EDGE DURA-02:ordering)", verification: backstop}
  assumptions:
    - "FLAGGED UNRESOLVED EXPT-01 unclassified probe: inspect 16K PNG validity, cancellation and publication on real hardware."
  prohibitions:
    - {statement: "A failed export must not replace a previous successful destination", status: flagged-unverified}
  artifacts:
    - {path: Scripts/Export/DocumentPngExport.cs, provides: "Frozen-revision bounded PNG publication"}
    - {path: tests/Mapwright.ContractTests/ExportRecoveryContracts.cs, provides: "Publication and fresh-process interruption contracts"}
  key_links:
    - {from: Scripts/Export/DocumentPngExport.cs, to: Scripts/Rendering/ConnectedTerrainGraph.cs, via: "Same immutable revision evaluator and bounded bands"}
    - {from: src/Mapwright.Infrastructure/SqliteProjectRepository.cs, to: Tools/StorageCrashWorker/Program.cs, via: "Fresh-process WAL/blob verification after forced termination"}
---
<objective>
Make export publication and crash recovery safe for the connected document.
Purpose: a responsive 16K PNG job and a killed process cannot turn acknowledged work or the previous export into partial state.
Output: frozen export service, failure-point worker and restart contracts.
</objective>
<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-connected-imported-terrain/01-CONTEXT.md
@.planning/phases/01-connected-imported-terrain/01-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-VALIDATION.md
@.planning/phases/01-connected-imported-terrain/01-05-SUMMARY.md
@.planning/phases/01-connected-imported-terrain/01-06-SUMMARY.md
@Scripts/Export/TerrainPipelineExportProbe.cs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Export a frozen revision to a validated bounded PNG</name>
  <files>Scripts/Export/DocumentPngExport.cs, Scripts/Export/StreamingPngWriter.cs, Scripts/Export/PngValidator.cs, tests/Mapwright.ContractTests/ExportRecoveryContracts.cs</files>
  <read_first>Scripts/Rendering/ConnectedTerrainGraph.cs; Scripts/Rendering/RenderResourceLedger.cs; Scripts/Export/TerrainPipelineExportProbe.cs; Scripts/Export/StreamingPngWriter.cs; Scripts/Export/PngValidator.cs; src/Mapwright.Application/EditSession.cs; tests/Mapwright.ContractTests/Program.cs; .planning/REQUIREMENTS.md</read_first>
  <behavior>Tests cover frozen revision despite later edits, pinned source/texture blobs, 16K dimension/CRC/inflated-scanline validation, tile/halo crop, cancellation at each phase, disk-full/invalid destination, previous-file preservation and real progress units.</behavior>
  <action>Create DocumentPngExport around a captured immutable revision and pinned asset set. Render bounded tiles/halos/bands using ConnectedTerrainGraph and RenderResourceLedger with interaction priority over export. Stream one cropped row at a time through StreamingPngWriter; flush file and directory as supported, validate signature/CRC/dimensions/decompressed length plus required colour/alpha metadata, then publish with a same-directory temporary sibling and atomic replace. Cancellation must be checked during rendering, encoding and validation and must remove only this job's temporary sibling. Keep prior destination byte-identical on cancellation/failure. Emit real phase/units and frozen revision. A valid 16K export is required; duration is observation only. Do not insert Phase 4 DPI, label or grid settings (EXPT-01, D-27). Register ExportPublication in DocumentPngExport.cs through the Plan 01 connected case registry; execute publication, frozen-revision and failure assertions, return a nonzero assertion count and propagate failures. Assert -ListCases contains it before invocation.</action>
  <verify><automated>./Scripts/run-connected-phase1.ps1 -Case ExportPublication</automated><fails_when>Destination changes on cancel/failure, PNG validation fails, full-frame GPU or >512 MiB export buffer is allocated, or later edits alter the frozen output.</fails_when></verify>
  <acceptance_criteria>One frozen revision exports a validated seamless PNG up to 16,384 per axis with bounded buffers, responsive cancellation and unchanged prior destination on failure.</acceptance_criteria>
  <done>Connected export is a safe publication job with observable progress.</done>
</task>
<task type="auto" tdd="true">
  <name>Recover acknowledged work after forced process or device loss</name>
  <files>src/Mapwright.Infrastructure/SqliteProjectRepository.cs, Tools/StorageCrashWorker/Program.cs, tests/Mapwright.ContractTests/ExportRecoveryContracts.cs</files>
  <read_first>src/Mapwright.Infrastructure/SqliteProjectRepository.cs; Tools/StorageCrashWorker/Program.cs; Scripts/Core/DurableEditRecoveryProbe.cs; Scripts/Rendering/DeviceLossProbe.cs; tests/Mapwright.ContractTests/ExportRecoveryContracts.cs; .planning/phases/01-connected-imported-terrain/01-CONTEXT.md</read_first>
  <behavior>Tests kill a child process before/after blob flush, before/after SQLite commit, after ack but before GPU render, and during temporary PNG write/validation/publication; a new process checks hashes, last acknowledged command/cursor and old or complete new destination. Empty and repeated recovery also pass.</behavior>
  <action>Harden blob publication: SHA-256 hash, verify, flush and durably rename each immutable blob before a WAL synchronous=FULL transaction references it. On startup validate referenced blobs, schema and cursor; ignore or safely collect unreferenced staged blobs only after verifying exact ownership. The worker must force termination at explicit stages and a separate fresh process must reopen; no managed cleanup, post-loss save, GPU readback or in-process device recreation is counted as recovery (DURA-01/DURA-02/D-27). Store enough recovery facts for the non-blocking D-26 banner, without guessing an exact lost gesture. Keep open SQLite WAL with the project; do not copy its main database file alone. Verify prior export survives a killed writer or device-loss process. Register CrashRecovery in SqliteProjectRepository.cs through the Plan 01 connected case registry; invoke the crash worker and fresh-process assertions, return a nonzero assertion count and propagate failures. Assert -ListCases contains it before invocation.</action>
  <verify><automated>./Scripts/run-connected-phase1.ps1 -Case CrashRecovery</automated><fails_when>Any acknowledged command or cursor is missing, a referenced blob hash fails, a partial PNG replaces the destination, or the test relies on post-loss in-process cleanup.</fails_when></verify>
  <acceptance_criteria>Fresh-process restart restores all acknowledged edits/cursor and reports only proven recovery facts; interrupted exports leave prior destination intact.</acceptance_criteria>
  <done>The durable project and export publication survive every tested forced interruption.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQLite/blob staging → acknowledged revision | Data must be durable and hash-valid before acknowledgement. |
| Frozen render → user destination | A temporary PNG crosses into published filesystem state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-22 | Tampering | referenced blobs | high | mitigate | Hash/flush/verify before SQLite reference; verify again on reopen. |
| T-01-23 | Tampering | PNG publication | high | mitigate | Same-directory temporary, full validator, flush and atomic replace only after success. |
| T-01-24 | Denial of service | export buffers | high | mitigate | Ledger-backed bounded bands/concurrency with cancellation checks. |
| T-01-25 | Elevation of privilege | output path | high | mitigate | Canonical path and source/project overwrite refusal, including symlink/junction recheck before publish. |
</threat_model>
<verification>Run ExportPublication and CrashRecovery cases, plus the full 16K real-source export gate. Record peak memory and destination hash before/after every injected failure.</verification>
<success_criteria>Export and restart are correct under frozen revisions, later edits, cancellation, disk failure and forced termination.</success_criteria>
## Artifacts this phase produces
- DocumentPngExport with frozen capture, progress/cancellation and temporary-sibling publication.
- StorageCrashWorker connected interruption stages.
- ExportRecoveryContracts for 16K, destination preservation and fresh-process durability.
<output>Create .planning/phases/01-connected-imported-terrain/01-07-SUMMARY.md when done.</output>
