---
phase: 01-connected-imported-terrain
plan: 13
type: execute
gap_closure: true
wave: 11
depends_on: [01-11, 01-12]
files_modified:
  - src/Mapwright.Application/HistorySession.cs
  - src/Mapwright.Infrastructure/SqliteProjectRepository.cs
  - Scripts/Acceptance/ConnectedAcceptance.cs
  - tests/Mapwright.ContractTests/HistoryContracts.cs
requirements: [HIST-01, HIST-02, HIST-04, DURA-01, REND-04]
autonomous: true
estimate: {tokens: 23000, raw_tokens: 23000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Recent ≤16-tile resident undo p95 is ≤100 ms on recorded hardware, with durable cursor and restart behavior unchanged."
    - "Undo and redo each produce sequence/revision-correlated presentation samples; no missing sample can pass."
    - "Older evicted reconstruction still exposes progress/cancel and never publishes a partial revision."
---
<objective>Remove the measured resident-undo bottleneck and complete UndoRedo presentation correlation.</objective>
<tasks>
<task type="auto">
  <name>Repair and benchmark resident undo</name>
  <files>src/Mapwright.Application/HistorySession.cs, src/Mapwright.Infrastructure/SqliteProjectRepository.cs, tests/Mapwright.ContractTests/HistoryContracts.cs</files>
  <read_first>src/Mapwright.Application/HistorySession.cs; src/Mapwright.Infrastructure/SqliteProjectRepository.cs; tests/Mapwright.ContractTests/HistoryContracts.cs; .planning/phases/01-connected-imported-terrain/01-11-SUMMARY.md</read_first>
  <action>Use Plan 11's stage timings to isolate resident-delta application versus SQLite cursor persistence. Optimize the measured hot path without acknowledging an undo before the durable cursor transaction; preserve redo invalidation, restart replay, and cancellation behavior. Add a repeated ≤16-tile resident benchmark with nearest-rank p95 and a threshold of 100 ms on recorded hardware.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>Nonzero exit, zero history contracts discovered, or cursor/restart invariants regress.</fails_when></verify>
  <acceptance_criteria>The measured resident operation meets 100 ms p95 and the cursor remains durable across process restart.</acceptance_criteria>
</task>
<task type="auto">
  <name>Correlate undo/redo to canvas draw</name>
  <files>Scripts/Acceptance/ConnectedAcceptance.cs</files>
  <read_first>Scripts/Acceptance/ConnectedAcceptance.cs; Scripts/App/MapCanvas.cs; .planning/phases/01-connected-imported-terrain/01-11-SUMMARY.md</read_first>
  <action>Send each undo and redo revision through the same dirty-tile canvas path as edits. Join sequence to exact revision and draw generation before FramePostDraw, including the initial setup and repeated cursor-revision cycles. Keep setup samples excluded with an explicit reason; never accept zero correlated UndoRedo samples.</action>
  <verify><automated>./Scripts/run-phase1-acceptance.ps1 -SelfTest</automated><fails_when>Nonzero exit, or repeated undo/redo revisions are spuriously matched to an old draw generation.</fails_when></verify>
  <acceptance_criteria>A short real-Vulkan UndoRedo diagnostic yields nonzero, causally matched samples and the resident p95 gate is separately reported.</acceptance_criteria>
</task>
<verification>Run history contracts, CrashRecovery, and short warm/cold/evicted UndoRedo diagnostics.</verification>
<success_criteria>HIST-02 performance and REND-04 presentation evidence are measured rather than inferred.</success_criteria>
## Artifacts this phase produces
- Resident-history benchmark and corrected UndoRedo presentation samples.
<output>Create 01-13-SUMMARY.md with durability and latency evidence.</output>
