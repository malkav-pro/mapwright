---
phase: 01-connected-imported-terrain
plan: 02
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - .planning/phases/01-connected-imported-terrain/01-Q1-DECISION.md
  - Scripts/Core/InkImportService.cs
  - Scripts/Core/ProjectStore.cs
  - src/Mapwright.Application/ImportProject.cs
  - tests/Mapwright.ContractTests/ImportContracts.cs
requirements: [DOC-01, DOC-03, IMPT-01, IMPT-02, IMPT-03]
autonomous: true
estimate: {tokens: 49000, raw_tokens: 49000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Every import presents editable recovered terrain and original flattened appearance with neither selected, then creates one versioned project revision in the configured folder or chosen override (D-01 to D-05)."
    - "Trusted source bases retain dimensions, transforms, IDs/version, hashes and baked-effect provenance; extras and untested commands are preserved and reported without guessed replay."
    - "Equal or touching imported raster bounds remain separate according to source order and transforms (EDGE IMPT-02:adjacency)."
    - "No trusted terrain base yields a readable partial/visual-only result with the original preview preserved (EDGE IMPT-02:empty)."
    - {statement: "Equal-order source rasters preserve a deterministic source-ID tie break after import (EDGE IMPT-02:ordering)", verification: backstop}
    - "Deleted disposable caches rebuild from source bytes and native commands; a repeated deletion is safe (EDGE DOC-03:idempotency)."
    - {statement: "Cache deletion during another read or export cannot remove a pinned source or native revision (EDGE DOC-03:concurrency)", verification: backstop}
    - {statement: "Exactly adjacent cache entries preserve render identity (EDGE DOC-03:adjacency)", verification: backstop}
    - {statement: "An absent cache directory rebuilds without source or edit loss (EDGE DOC-03:empty)", verification: backstop}
    - {statement: "Equal-key cache rebuild results retain stable revision order (EDGE DOC-03:ordering)", verification: backstop}
  assumptions:
    - "FLAGGED UNRESOLVED IMPT-03 unclassified probe: review each hostile decoder limit and user-facing degradation on the real format."
  prohibitions:
    - {statement: "Partial recovery must not be labelled fully editable when untested source commands affect terrain", status: flagged-unverified}
  artifacts:
    - {path: src/Mapwright.Application/ImportProject.cs, provides: "Recovery-choice-to-authoritative-project use case"}
    - {path: tests/Mapwright.ContractTests/ImportContracts.cs, provides: "Bounded import and provenance contracts"}
  key_links:
    - {from: Scripts/Core/InkImportService.cs, to: src/Mapwright.Application/ImportProject.cs, via: "Bounded parsed source and verified raster descriptors"}
    - {from: src/Mapwright.Application/ImportProject.cs, to: src/Mapwright.Infrastructure/SqliteProjectRepository.cs, via: "One initial revision with source/blob references"}
---

<objective>
Complete both honest recovery modes, trusted two-role raster mapping, source preservation and bounded hostile-input outcomes.
Purpose: the imported .ink remains provenance while the selected mode initializes one authoritative native document, as established by plan 01.
Output: import use case, bounded decoder, recovery report and contracts.
</objective>
<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-connected-imported-terrain/01-CONTEXT.md
@.planning/phases/01-connected-imported-terrain/01-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-UI-SPEC.md
@.planning/phases/01-connected-imported-terrain/01-01-SUMMARY.md
@src/Mapwright.Domain/MapModel.cs
@src/Mapwright.Infrastructure/SqliteProjectRepository.cs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Map verified source terrain or locked preview into one project</name>
  <files>Scripts/Core/InkImportService.cs, Scripts/Core/ProjectStore.cs, src/Mapwright.Application/ImportProject.cs, tests/Mapwright.ContractTests/ImportContracts.cs, .planning/phases/01-connected-imported-terrain/01-Q1-DECISION.md</files>
  <read_first>Scripts/Core/InkImportService.cs; Scripts/Core/MapDocument.cs; Scripts/Core/ProjectStore.cs; src/Mapwright.Domain/MapModel.cs; src/Mapwright.Infrastructure/SqliteProjectRepository.cs; tests/Mapwright.ContractTests/Program.cs; .planning/phases/01-connected-imported-terrain/01-CONTEXT.md; .planning/phases/01-connected-imported-terrain/01-UI-SPEC.md</read_first>
  <behavior>Tests cover both unselected-mode choices, source hash/transform/ID/version persistence, empty and multiple terrain bases, equal-order raster tie break, out-of-order/untested command replay cutoff, and preserving unsupported metadata.</behavior>
  <decision_gate id="Q1" type="evidence" before="editable-import">Before implementing editable mapping, inspect the real .ink fixture and record its SHA-256, source version, raster IDs/order, intrinsic dimensions, transforms, decoded colour/coverage semantics and replay cutoff in 01-Q1-DECISION.md. Compare each candidate mapping against original preview pixels and source command order. Choose per-raster trusted Background/Foreground mapping only where deterministic correspondence is demonstrated; otherwise choose partial recovery with the preserved flattened visual fallback under D-05. Record accepted and rejected candidates, exact evidence and tests to enforce the choice. Halt editable mapping if neither source inspection nor preserved visual fallback can be verified.</decision_gate>
  <action>Extend import parsing to retain complete source descriptors and unsupported payloads. Inspect the real .ink fixture to define versioned, tested Background colour, Foreground colour and coverage mappings, including intrinsic raster dimensions and explicit source transforms. Create editable bases only when those semantics are trusted; report extra/unmappable terrain as partial and always retain the baked-preview choice. Keep original bytes, hashes and raster provenance in the existing SQLite-backed project created in plan 01; adapt ProjectStore staging/hash helpers without a second authoritative manifest. The application use case enforces D-01/D-02 mode choice before publication, configured projects-folder default and override per D-03, and D-05 two-role mapping. Put untested source state-changing commands behind a trusted-replay cutoff for the affected content. Treat recovery mode as a variant of one project document, not a separate source of truth.</action>
  <verify><automated>$p='.planning/phases/01-connected-imported-terrain/01-Q1-DECISION.md'; if (!(Test-Path $p) -or !(Select-String -Path $p -Pattern '^Decision: .+' -Quiet) -or !(Select-String -Path $p -Pattern '^Evidence: .+' -Quiet)) { exit 1 }; ./Scripts/run-contract-tests.ps1</automated><fails_when>Q1 lacks measured source evidence and an explicit mapping/fallback disposition, any import contract fails, or a project loses source bytes/provenance or invents trusted terrain from unmappable input.</fails_when></verify>
  <acceptance_criteria>Both modes produce the same fixed role structure; flattened mode locks preview Background and starts empty Foreground; editable mode uses only verified raster bases; every extra source object remains reportable and inspectable.</acceptance_criteria>
  <done>Real source data can be imported in either mode without changing the original or creating extra editable terrain.</done>
</task>
<task type="auto" tdd="true">
  <name>Bound import and cache-recovery edge cases</name>
  <files>Scripts/Core/InkImportService.cs, Scripts/Core/ProjectStore.cs, src/Mapwright.Application/ImportProject.cs, tests/Mapwright.ContractTests/ImportContracts.cs</files>
  <read_first>Scripts/Core/InkImportService.cs; Scripts/Core/ProjectStore.cs; tests/Mapwright.ContractTests/ImportContracts.cs; .planning/REQUIREMENTS.md; .planning/phases/01-connected-imported-terrain/01-VALIDATION.md</read_first>
  <behavior>Tests cover compressed/expanded byte, token, nesting, base64, image dimension/pixel and aggregate allocation limits before allocation; malformed and declared-oversize input; repeated/concurrent cache deletion; cache-free reopen and export within tolerance.</behavior>
  <action>Set explicit checked budgets at every decompression, JSON token, depth, base64 and raster allocation boundary, with concise rejection or safe preview-only degradation and unchanged source. Use canonical paths and source/project separation for the configured destination and override. Make cache deletion target only recorded disposable render/history acceleration directories after exact-path verification; source/blob/history references remain immutable and pinned. Return actionable outcome text that Main can show. The cache operation should be repeatable and should refuse deletion while a pinned reader requires an entry. Implement DOC-03 and IMPT-03 edge behavior without treating a fixture-only parser pass as proof of whole import. In ImportProject.cs, register ImportBounds through the Plan 01 connected case registry; the handler must execute the hostile-input and cache-free reopen/export assertions, return their nonzero count and propagate failure. Assert -ListCases contains ImportBounds before invoking it.</action>
  <verify><automated>./Scripts/run-connected-phase1.ps1 -Case ImportBounds</automated><fails_when>A hostile fixture exceeds a configured allocation before rejection, source bytes change, a cache-free reopen/export differs beyond tolerance, or a failure returns success.</fails_when></verify>
  <acceptance_criteria>Oversize/corrupt sources produce bounded readable outcomes; deleting all disposable caches leaves original blobs/native commands intact and reconstructs the same committed rendering.</acceptance_criteria>
  <done>Import and cache rebuild are safe under both ordinary and adversarial local inputs.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| .ink → bounded parser | Hostile gzip/JSON/base64/PNG data controls declared sizes and fields. |
| Import metadata → local paths | Source strings and project names can influence output paths. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05 | Denial of service | decoder allocation | high | mitigate | Checked byte/token/depth/pixel/aggregate budgets before each allocation. |
| T-01-06 | Tampering | imported source references | high | mitigate | Hash verify immutable source/raster blobs on import and reopen. |
| T-01-07 | Elevation of privilege | project path | high | mitigate | Resolve canonical paths; reject source overwrite and escapes via metadata. |
| T-01-08 | Repudiation | recovery report | medium | mitigate | Persist source version, IDs, skipped counts and replay cutoff with initial revision. |
</threat_model>
<verification>Run contracts and connected ImportBounds case with the real fixture plus malformed/decompression fixtures. Check the report against preserved source hashes.</verification>
<success_criteria>Both recovery modes initialize exactly two roles; source/provenance survive; bounded hostile inputs and cache deletion cannot remove native edits.</success_criteria>
## Artifacts this phase produces
- ImportProject recovery-mode application use case and persistent recovery report.
- ImportContracts fixture cases and -Case ImportBounds runner branch.
- Versioned source-to-role mapping table derived from the real .ink fixture.
<output>Create .planning/phases/01-connected-imported-terrain/01-02-SUMMARY.md when done.</output>
