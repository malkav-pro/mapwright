---
phase: 01-connected-imported-terrain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Scripts/App/Main.cs
  - Scripts/App/MapCanvas.cs
  - src/Mapwright.Domain/MapModel.cs
  - src/Mapwright.Domain/Commands.cs
  - src/Mapwright.Infrastructure/SqliteProjectRepository.cs
  - Scripts/Rendering/ConnectedTerrainGraph.cs
  - Scripts/run-connected-phase1.ps1
  - tests/Mapwright.ContractTests/Program.cs
autonomous: true
requirements: [DOC-01, DOC-02, IMPT-01, LAYR-01, TERR-01, EXPT-01, DURA-01, REND-01]
estimate: {tokens: 82000, raw_tokens: 82000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "One real .ink can enter the original-flattened recovery choice, create one project, accept one Background or Foreground texture stroke durably, reopen its acknowledged revision, show that revision, and export its pixels through the same render evaluator."
    - "The imported .ink stays byte-identical; the project has exactly two terrain identities and no second authoritative manifest (D-05, D-06)."
    - "A repeated Save with no queued command preserves the same revision (EDGE DOC-02:idempotency)."
    - "Two commit attempts on the same base revision yield one acknowledged successor and one revision conflict (EDGE DOC-02:concurrency)."
    - "The connected runner enumerates registered cases, dispatches Tracer to a real end-to-end assertion, and returns nonzero for an unknown case, a missing handler or zero assertions."
  assumptions:
    - "FLAGGED UNRESOLVED DOC-01 unclassified probe: inspect document size, identity, and imported-transform edge cases during the connected smoke run."
    - "FLAGGED UNRESOLVED IMPT-01 unclassified probe: review whether the real preview is safe and legible in locked Background mode."
  prohibitions:
    - {statement: "Imported flattened pixels must not be presented as editable recovered coastline geometry", status: flagged-unverified}
  artifacts:
    - {path: Scripts/Rendering/ConnectedTerrainGraph.cs, provides: "Shared committed-revision evaluation for canvas and PNG"}
    - {path: Scripts/run-connected-phase1.ps1, provides: "Real import-edit-reopen-render-export smoke invocation"}
  key_links:
    - {from: Scripts/App/MapCanvas.cs, to: src/Mapwright.Application/EditSession.cs, via: "Pointer release submits one command and waits for durable acknowledgement"}
    - {from: Scripts/Rendering/ConnectedTerrainGraph.cs, to: src/Mapwright.Infrastructure/SqliteProjectRepository.cs, via: "Reopened immutable revision and verified source blob"}
---

<objective>
Establish the production connected tracer: real source import, one texture gesture, durable commit, process-independent reopen, viewport render, and PNG export of one authoritative revision.
Purpose: prove the selected import mode is a variant of one project/revision, not a second document or authority. Promote the existing EditSession and SQLite path as authority; the older ProjectStore manifest is legacy input only. This is an add-alongside source-preserving transition, not two independent document stores.
Output: a runnable connected smoke path and shared render evaluator.
</objective>

<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-connected-imported-terrain/01-CONTEXT.md
@.planning/phases/01-connected-imported-terrain/01-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-UI-SPEC.md
@src/Mapwright.Application/EditSession.cs
@Scripts/Core/InkImportService.cs
@Scripts/Export/StreamingPngWriter.cs
@Scripts/Export/PngValidator.cs
</context>

<tasks>
<task type="tracer">
  <name>Prove the real flattened import through durable edit, reopen, viewport and PNG</name>
  <files>Scripts/App/Main.cs, Scripts/App/MapCanvas.cs, src/Mapwright.Domain/MapModel.cs, src/Mapwright.Domain/Commands.cs, src/Mapwright.Infrastructure/SqliteProjectRepository.cs, Scripts/Rendering/ConnectedTerrainGraph.cs, Scripts/run-connected-phase1.ps1</files>
  <read_first>Scripts/App/Main.cs; Scripts/App/MapCanvas.cs; src/Mapwright.Domain/MapModel.cs; src/Mapwright.Domain/Commands.cs; src/Mapwright.Application/EditSession.cs; src/Mapwright.Application/Ports.cs; src/Mapwright.Infrastructure/SqliteProjectRepository.cs; Scripts/Core/InkImportService.cs; Scripts/Core/ProjectStore.cs; Scripts/Export/StreamingPngWriter.cs; Scripts/Export/PngValidator.cs; .planning/phases/01-connected-imported-terrain/01-CONTEXT.md; .planning/phases/01-connected-imported-terrain/01-UI-SPEC.md</read_first>
  <action>Implement one real flattened-mode path using the existing importer and a source-preserving blob reference in the SQLite project. Present the quick preview/recovered-summary review before the user chooses the flattened card, and leave Create project disabled until a mode is selected per D-01/D-02. For this tracer exercise that card only; later import expansion completes editable recovery. Create fixed Background below Foreground identities (D-06/D-07), with the preview locked in Background and empty Foreground (D-05). Remove the mandatory dummy river from the domain shape while retaining compatibility for existing probe data; version the stored revision format so subsequent expansion can migrate it. Route a single Foreground texture gesture through EditSession; provisional pixels are disposable and release acknowledges only after SQLite commit. Implement one revision-keyed evaluator in ConnectedTerrainGraph, used by the canvas and by a bounded PNG row path with the existing writer/validator. Store source bytes, hash and revision references before acknowledgement; do not let the old manifest become a second authority. A command-line connected-smoke entry in Main must import the real configured fixture, edit, dispose/reopen repository in a fresh process, compare rendered viewport sampling and PNG from the reopened revision, and exit nonzero on mismatch. Create run-connected-phase1.ps1 with a required -Case argument and a discoverable case registry in Main: each handler has a unique case name and returns a count of executed assertions; -ListCases prints names; -Case resolves exactly one handler, invokes it, and propagates nonzero failure. Register Tracer in Main. Unknown, duplicate, unimplemented and zero-assertion cases must exit nonzero, with no success fallback. Later plans register handlers in their owned production/smoke classes through this registry without editing Main or the runner. Use pinned runtime paths. Keep the Godot/SQLite inward boundaries described by RESEARCH; the graph consumes immutable snapshots, never edits storage directly. D-27 applies to acknowledgement order.</action>
  <verify><automated>./Scripts/run-connected-phase1.ps1 -Case Tracer; if ($LASTEXITCODE -ne 0) { exit 1 }; ./Scripts/run-connected-phase1.ps1 -Case __UnknownCaseMustFail__; if ($LASTEXITCODE -eq 0) { exit 1 }</automated><fails_when>The real import, durable commit, fresh-process reopen, viewport sample or PNG result fails or disagrees; Tracer has no assertions; or an unknown case succeeds.</fails_when></verify>
  <acceptance_criteria>One source hash and one project identity are stored; the original source is untouched; the chosen flattened mode produces two fixed terrain roles; the acknowledged edit reopens and gives equal viewport/export samples; -ListCases includes Tracer; missing blobs, a different revision, an unregistered name and a zero-assertion handler exit nonzero.</acceptance_criteria>
  <done>The connected path uses the existing application session and one SQLite authority, and a real user can complete the first import/edit/save/reopen/export path.</done>
</task>
<task type="auto">
  <name>Adapt the contract runner to the two-role connected baseline</name>
  <files>tests/Mapwright.ContractTests/Program.cs</files>
  <read_first>tests/Mapwright.ContractTests/Program.cs; Scripts/run-contract-tests.ps1; src/Mapwright.Domain/MapModel.cs; src/Mapwright.Domain/Commands.cs; .planning/phases/01-connected-imported-terrain/01-VALIDATION.md</read_first>
  <action>Replace the historical four-terrain fixture with Background/Foreground and update existing assertions for the accepted mask/river semantics. Add a deterministic registry/discovery seam for focused contract classes created in subsequent plans so run-contract-tests.ps1 executes them without repeated edits to Program.cs. Preserve the existing meaningful commit-before-render and failed-commit assertions. Require a nonzero test count and nonzero exit on any failure; runner output must identify each case. The old synthetic probe results remain separately labelled.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>No tests are discovered, an existing contract fails, or a new contract class can be silently skipped.</fails_when></verify>
  <acceptance_criteria>All existing contracts run against two fixed roles, and new focused classes are discovered without modifying Program.cs again.</acceptance_criteria>
  <done>The contract runner is ready to enforce each expansion plan.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| .ink file → importer | Untrusted compressed/JSON/raster data enters native process. |
| Editor → SQLite/blob store | Acknowledgement and references cross the durable-storage boundary. |
| Render graph → PNG path | User-selected output may replace an existing local file. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | source/blob reference | high | mitigate | SHA-256 verify source copy on create/reopen and keep original immutable. |
| T-01-02 | Tampering | SQLite revision | high | mitigate | WAL FULL transaction and base-revision compare before acknowledgement. |
| T-01-03 | Denial of service | import decoder | high | mitigate | Apply existing parser bounds for tracer and complete explicit limits in plan 02. |
| T-01-04 | Information disclosure | PNG destination | medium | mitigate | Validate selected destination and avoid source/project path overwrite. |
</threat_model>

<verification>
Run both ./Scripts/run-contract-tests.ps1 and ./Scripts/run-connected-phase1.ps1 -Case Tracer. Report fixture hash and actual output evidence; passing legacy probes alone is insufficient.
</verification>
<success_criteria>One real import, committed stroke, process-independent reopen, viewport evaluation and PNG evaluation share one revision and one SQLite-backed project.</success_criteria>

## Artifacts this phase produces
- ConnectedTerrainGraph and revision-keyed render entry points.
- Connected smoke command-line entry and run-connected-phase1.ps1 with -Case Tracer.
- Versioned SQLite source/blob references for the initial project.

<output>Create .planning/phases/01-connected-imported-terrain/01-01-SUMMARY.md when done.</output>
