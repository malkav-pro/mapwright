---
phase: 01-connected-imported-terrain
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - src/Mapwright.Domain/MapModel.cs
  - src/Mapwright.Domain/Commands.cs
  - src/Mapwright.Domain/Geometry.cs
  - tests/Mapwright.ContractTests/MaskRiverContracts.cs
requirements: [MASK-01, WATR-01, HIST-04, REND-01]
autonomous: true
estimate: {tokens: 52000, raw_tokens: 52000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Land Add/Subtract changes only Foreground coverage using Edged polygon with Roughness/Smooth corners or Round soft with Softness (D-08/D-14/D-15)."
    - "A river centreline, per-point width profile and bank softness subtract after painted coverage, so later Land Add cannot close it (D-08/D-21)."
    - "Land coverage at zero, half and full strength and one step beyond each limit has specified behavior (EDGE MASK-01:boundary)."
    - {statement: "Coverage 0.5 contour rasterization uses one declared transform and tie policy (EDGE MASK-01:precision)", verification: backstop}
    - "River width and bank softness at their min/max and one step beyond have specified behavior (EDGE WATR-01:boundary)."
    - {statement: "River width/bank raster rounding uses the declared document-to-output transform (EDGE WATR-01:precision)", verification: backstop}
  artifacts:
    - {path: tests/Mapwright.ContractTests/MaskRiverContracts.cs, provides: "Mask/river ordering, boundary and replay fixtures"}
  key_links:
    - {from: src/Mapwright.Domain/Commands.cs, to: src/Mapwright.Domain/MapModel.cs, via: "Mask stroke and river modifier ordering with old/new invalidation bounds"}
---
<objective>
Add native Foreground coverage painting and editable river subtraction with deterministic geometry and undoable commands.
Purpose: land, texture, whole-layer opacity and non-destructive water must stay semantically distinct.
Output: validated Land and river records/commands with boundary fixtures.
</objective>
<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-connected-imported-terrain/01-CONTEXT.md
@.planning/phases/01-connected-imported-terrain/01-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-UI-SPEC.md
@.planning/phases/01-connected-imported-terrain/01-03-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Paint the single Foreground land mask with two distinct footprints</name>
  <files>src/Mapwright.Domain/MapModel.cs, src/Mapwright.Domain/Commands.cs, src/Mapwright.Domain/Geometry.cs, tests/Mapwright.ContractTests/MaskRiverContracts.cs</files>
  <read_first>src/Mapwright.Domain/MapModel.cs; src/Mapwright.Domain/Commands.cs; src/Mapwright.Domain/Geometry.cs; tests/Mapwright.ContractTests/BrushContracts.cs; .planning/phases/01-connected-imported-terrain/references/land-edged-brush-reference.png; .planning/phases/01-connected-imported-terrain/references/land-round-brush-reference.png; .planning/phases/01-connected-imported-terrain/01-CONTEXT.md</read_first>
  <behavior>Tests distinguish texture stroke from coverage stroke; Edged Smooth changes corner geometry while keeping firm alpha, Round soft changes falloff; Add/Subtract reveal Background; exactly 0, 0.5 and 1 coverage and neighboring values remain stable across tiles and saved replay.</behavior>
  <action>Add resolved Land-stroke records and commands targeting only Foreground's coverage. Implement Edged polygon painting footprint with Roughness and corner-rounding Smooth, and Round soft footprint with Softness; both have Add/Subtract and document-pixel diameter (D-14/D-15). A stroke is a painting gesture, not a click-to-place polygon. Persist shape/parameters/random seed/version and document-space samples. Enforce D-07/D-08 separation from Background, texture colour and layer opacity; no independent mask-layer object. Compute affected bounds and coverage invalidation conservatively. Choose numeric limits from geometry/render budgets and test inclusive/exclusive boundaries. Do not add styling UI or the rejected square Land mode.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>A Land stroke changes texture/opacity, Background receives coverage, edge smoothing acts as alpha softness, or tiled contour fixtures diverge.</fails_when></verify>
  <acceptance_criteria>Both Land modes and Add/Subtract commit deterministic Foreground-only coverage; subtract reveals Background; Softness and corner Smooth produce different documented geometry.</acceptance_criteria>
  <done>Users can shape one Foreground land mask with the two approved footprints.</done>
</task>
<task type="auto" tdd="true">
  <name>Edit a river that remains open after land painting</name>
  <files>src/Mapwright.Domain/MapModel.cs, src/Mapwright.Domain/Commands.cs, src/Mapwright.Domain/Geometry.cs, tests/Mapwright.ContractTests/MaskRiverContracts.cs</files>
  <read_first>src/Mapwright.Domain/MapModel.cs; src/Mapwright.Domain/Commands.cs; src/Mapwright.Domain/Geometry.cs; tests/Mapwright.ContractTests/MaskRiverContracts.cs; .planning/phases/01-connected-imported-terrain/01-UI-SPEC.md</read_first>
  <behavior>Tests create/move/delete river points, edit per-point widths and bank softness, toggle/delete modifier, union old/new bounds, cross tile borders, and assert that a later Land Add cannot refill the channel.</behavior>
  <action>Represent river centreline points with a width profile, per-point width, bank softness, enabled state and stable identity. Add one-command operations for point drag, width drag, Apply width to all, softness, enable and deletion; each validates range and unions old/new invalidation bounds. Evaluate the river as a non-destructive subtraction after all Land strokes (D-08/D-21). Preserve river parameters in native history and make point/river deletion undoable without a confirmation gate. Give the renderer explicit river geometry so a later optional coast-style decision can distinguish banks; do not equate sea connectivity with coastline classification. Defer lake creation to Phase 3 while ensuring this modifier order permits it.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>A late Land stroke closes the river, an edited width retains stale tiles, a deleted river cannot be undone, or out-of-range dimensions commit.</fails_when></verify>
  <acceptance_criteria>River points, width profile and bank softness edit live; river subtraction reveals Background at tile crossings and survives later Land painting/reopen.</acceptance_criteria>
  <done>River modification is a native, undoable part of Foreground coverage composition.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Pointer/numeric input → geometry | Unbounded or non-finite coordinates could corrupt persisted commands or allocate excessive tiles. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12 | Tampering | mask/river target | high | mitigate | Validate Foreground ownership and immutable modifier order in domain commands. |
| T-01-13 | Denial of service | geometry bounds | medium | mitigate | Reject NaN/infinity/out-of-range width and checked tile bounds before render allocation. |
| T-01-14 | Repudiation | river edits | low | accept | Stable command IDs and SQLite history already attribute local actions; no remote actor exists. |
</threat_model>
<verification>Run focused contracts and connected mask/river case once the graph expansion is available; retain failing fixtures until renderer work completes.</verification>
<success_criteria>Mask and river edits are distinct from texture/opacity, deterministic and replayable, with river subtraction ordered last.</success_criteria>
## Artifacts this phase produces
- LandShape, LandStroke and river width/softness modifier records.
- Foreground-only coverage and river commands.
- MaskRiverContracts fixtures for contour/width boundaries, ordering and old/new invalidation.
<output>Create .planning/phases/01-connected-imported-terrain/01-04-SUMMARY.md when done.</output>
