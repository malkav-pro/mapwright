# 01-13 resident history and draw-correlation diagnostic

Status: **partial; HIST-02 and REND-04 remain failed**. This is not a plan-completion summary.

The acceptance recorder now starts the measured UndoRedo interval after the seeded setup revision has reached the matching canvas draw. It joins an Undo to exactly `baseRevision - 1`, a Redo to exactly `baseRevision + 1`, and both to a unique sequence and draw generation. A short warm real-Vulkan run produced two correlated samples, revisions 0 and 1, generations 3 and 4; it no longer reports an empty or uncorrelated row. The full 12-row, 60-second matrix has not been repeated.

`HistorySession` now reuses a previously reconstructed adjacent model only when a compatible resident delta exists and the target's durable command identity is unchanged. The cursor transaction still precedes publication. The baseline read avoids redundant checkpoint replay. An adjacent history change that is provably just one texture/coverage stroke now invalidates that stroke's bounds; unknown changes still invalidate the full map. The editor and acceptance recorder pass that bound to the same canvas presentation path. The 20-sample 16-tile contract benchmark passes a 100 ms assertion, and all 64 contracts pass, including bounded navigation, branch replacement, restart and cancellation behavior.

The earlier 13-second warm UndoRedo diagnostic remained far outside the target: first undo 154.98 ms, cached redo 11.21 ms; two full viewport render stages 5,646.03 and 5,457.10 ms. Raw evidence: `artifacts/phase1-11/diagnostic-undo-repeat.json`, SHA-256 `bdba3d1cf3051559a92a1832ec920a4db8e413c4a83823f74a74b5b83183d675`. After bounded history invalidation, a new 3-second warm diagnostic recorded two correlated samples: first undo 153.91 ms and redo 11.26 ms, with render stages 48.03 and 52.96 ms. Raw evidence: `artifacts/phase1-11/diagnostic-undo-bounded.json`, SHA-256 `abb3b4740c13dd963704afbc631c21d3ad13b3da1c360da05c861581c8122e3a`. This is a substantial redraw improvement but still fails the first-use resident-undo and interaction thresholds. Plan 01-13 remains open. No threshold was relaxed.

Subsequently, the adjacent resident-baseline load was changed to reuse the verified current revision's immutable blob identities while retaining schema/source metadata checks; ordinary load and reconstruction still rehash blobs. The first real-fixture undo fell to 33.02 ms in a one-sample check. With the flattened-base viewport fast path, a 13-second warm diagnostic produced four correlated UndoRedo samples and passed its unchanged short-run thresholds: input-to-draw p95 35.657 ms, resident-undo p95 32.07 ms, render stages 0.49–5.11 ms. Raw evidence: `artifacts/phase1-11/diagnostic-undo-copybase.json`, SHA-256 `3279a6cca890512afffc9eca727d2f50fe4a8542b17dea511bd73d2ac01d4c93`. The diagnostic is not a 60-second warm/cold/evicted result, so Plan 01-13 and the phase gate remain open.
