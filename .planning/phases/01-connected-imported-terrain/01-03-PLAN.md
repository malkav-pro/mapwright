---
phase: 01-connected-imported-terrain
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .planning/phases/01-connected-imported-terrain/01-Q2-DECISION.md
  - .planning/phases/01-connected-imported-terrain/01-Q3-DECISION.md
  - src/Mapwright.Domain/MapModel.cs
  - src/Mapwright.Domain/Commands.cs
  - src/Mapwright.Domain/Geometry.cs
  - tests/Mapwright.ContractTests/BrushContracts.cs
requirements: [DOC-01, LAYR-01, TERR-01, TERR-02, HIST-04]
autonomous: true
estimate: {tokens: 54000, raw_tokens: 54000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Exactly Background then Foreground are valid terrain roles; locks, visibility and solo are enforced by commands as well as UI (D-06/D-07/D-10)."
    - "One texture brush targets either role and preserves one shared set of settings across target switches (D-11); diameter stays in document pixels at every zoom (D-12)."
    - "Resolved preset, tip, texture identity/transform, taper, roughness, corner smoothing, seed and algorithm version replay identically after reopen (D-16/D-17/D-18)."
    - "A repeated layer-state command is idempotent or reports an explicit no-op (EDGE LAYR-01:idempotency)."
    - "Concurrent stale layer commands cannot silently overwrite the latest state (EDGE LAYR-01:concurrency)."
  assumptions:
    - "FLAGGED UNRESOLVED TERR-01 unclassified probe: compare full preset/texture replay including missing texture identity and deterministic randomness."
    - "FLAGGED UNRESOLVED TERR-02 unclassified probe: inspect cursor footprint and document anchoring at tile/zoom boundaries."
  artifacts:
    - {path: tests/Mapwright.ContractTests/BrushContracts.cs, provides: "Fixed-role and deterministic texture-brush contracts"}
  key_links:
    - {from: src/Mapwright.Domain/Commands.cs, to: src/Mapwright.Domain/MapModel.cs, via: "Validated resolved brush and fixed target role"}
---
<objective>
Expand the tracer to the approved two-role terrain command model and complete deterministic texture-paint semantics.
Purpose: native strokes must replay from resolved immutable data and must never mutate coverage or silently retarget.
Output: fixed-role commands, validated brush recipes and focused contracts.
</objective>
<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-connected-imported-terrain/01-CONTEXT.md
@.planning/phases/01-connected-imported-terrain/01-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-UI-SPEC.md
@.planning/phases/01-connected-imported-terrain/01-01-SUMMARY.md
@src/Mapwright.Application/EditSession.cs
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Enforce two terrain roles and paint-target guards</name>
  <files>src/Mapwright.Domain/MapModel.cs, src/Mapwright.Domain/Commands.cs, tests/Mapwright.ContractTests/BrushContracts.cs, .planning/phases/01-connected-imported-terrain/01-Q3-DECISION.md</files>
  <read_first>src/Mapwright.Domain/MapModel.cs; src/Mapwright.Domain/Commands.cs; src/Mapwright.Domain/Geometry.cs; tests/Mapwright.ContractTests/Program.cs; .planning/phases/01-connected-imported-terrain/01-CONTEXT.md</read_first>
  <behavior>Tests reject a third terrain role, terrain reorder/remove/duplicate, Background coverage, hidden/locked target edits and stale concurrent commands; they assert stable role IDs and separate visibility/solo/opacity/rename semantics.</behavior>
  <decision_gate id="Q3-solo" type="evidence" before="solo-command">Before implementing solo, compare Background-only, Foreground-only, both-solo and neither-solo outcomes against D-06/D-07 and UI-SPEC terrain-row wording. Select and record a precise visibility truth table and how lock/hidden state interacts with solo in 01-Q3-DECISION.md; use deterministic command tests as the acceptance oracle. If source contracts do not distinguish competing semantics, record the alternatives and a recommended rule with rationale for review before persisting it. The under-Background rendering decision is separately gated in plan 06 before composition.</decision_gate>
  <action>Replace the tracer's provisional terrain state with exactly two role-identified terrain records, Background below Foreground, one Foreground-owned coverage channel and stable imported transforms/IDs (D-06/D-07). Define commands for rename, visibility, lock, solo and layer opacity, with checked range and role validation. Reject paint on locked or hidden targets, returning a reason the UI can render with Unlock or Show layer per D-10; never reroute. Keep opacity independent of coverage. Preserve old stored revisions through a versioned migration/read path rather than replacing source files in place. Ensure equal repeated state requests do not fabricate an edit and stale base-revision requests conflict.</action>
  <verify><automated>$p='.planning/phases/01-connected-imported-terrain/01-Q3-DECISION.md'; if (!(Test-Path $p) -or !(Select-String -Path $p -Pattern '^Solo Decision: .+' -Quiet) -or !(Select-String -Path $p -Pattern '^Solo Evidence: .+' -Quiet)) { exit 1 }; ./Scripts/run-contract-tests.ps1</automated><fails_when>Q3 solo truth table is absent, a prohibited role/order/target change succeeds, opacity changes coverage, or old stored revisions fail to read.</fails_when></verify>
  <acceptance_criteria>Every project has one Background and one Foreground in fixed order; only Foreground can own coverage; each terrain row supports name/visible/lock/solo/opacity; blocked gestures create no command.</acceptance_criteria>
  <done>Role and targeting rules are enforced below the UI boundary.</done>
</task>
<task type="auto" tdd="true">
  <name>Persist resolved texture presets and document-space strokes</name>
  <files>src/Mapwright.Domain/MapModel.cs, src/Mapwright.Domain/Commands.cs, src/Mapwright.Domain/Geometry.cs, tests/Mapwright.ContractTests/BrushContracts.cs, .planning/phases/01-connected-imported-terrain/01-Q2-DECISION.md</files>
  <read_first>src/Mapwright.Domain/MapModel.cs; src/Mapwright.Domain/Commands.cs; src/Mapwright.Domain/Geometry.cs; tests/Mapwright.ContractTests/BrushContracts.cs; .planning/phases/01-connected-imported-terrain/references/brush-preset-reference.png; .planning/phases/01-connected-imported-terrain/references/texture-edged-brush-reference.png; .planning/phases/01-connected-imported-terrain/01-UI-SPEC.md</read_first>
  <behavior>Tests cover hard/soft round, tapered, square/pencil, irregular/textured and edged tip examples; one tapered mouse stroke narrows at both ends; non-tapered stays untapered; seeds replay; strokes cross a tile edge and retain the same document-space texture anchor; changing target does not change settings or coverage.</behavior>
  <decision_gate id="Q2" type="evidence" before="brush-recipe-persistence">Before fixing persistent recipe fields/defaults, inspect supplied reference images, required tip families, existing domain bounds and rendered stroke fixtures. Record candidate diameter/roughness/spacing/jitter bounds, defaults, inventory/assets and mouse taper curves in 01-Q2-DECISION.md, with rejection reasons and a chosen set supported by tile/resource/replay tests. Preserve D-16/D-17/D-18; no screenshot number is an authority. If no candidate satisfies deterministic replay and bounds, stop persistence of that parameter and report the failing fixture for a revised decision.</decision_gate>
  <action>Define one resolved texture recipe containing tip shape/version, diameter converted once to radius, hardness, opacity, flow, spacing, texture SHA-256, scale, rotation, jitter, roughness/corner smoothing when edged, taper curve/version, random seed/version and document-space samples (D-11/D-12/D-16/D-17/D-18). Choose and record a compact asset-backed preset inventory, numeric ranges/defaults and taper curve from validated domain/render limits; screenshot values are examples. Store resolved values in each command rather than only a mutable preset name. Keep texture-colour invalidation separate from coverage invalidation. Provide deterministic bounds including taper/jitter/rotation and refuse invalid numeric values. Texture painting never changes the land mask (D-08). The UI may later expose Save preset as, but the recipe must already be replayable if its library entry changes.</action>
  <verify><automated>$p='.planning/phases/01-connected-imported-terrain/01-Q2-DECISION.md'; if (!(Test-Path $p) -or !(Select-String -Path $p -Pattern '^Decision: .+' -Quiet) -or !(Select-String -Path $p -Pattern '^Evidence: .+' -Quiet)) { exit 1 }; ./Scripts/run-contract-tests.ps1</automated><fails_when>Q2 lacks a recorded measured parameter choice, identical recipes produce different samples/pixels, brush target changes settings, a texture stroke changes coverage, or invalid bounds enter history.</fails_when></verify>
  <acceptance_criteria>All required tip families and edged/tapered variants have resolved immutable recipes; committed texture strokes replay across restart, tiles, zoom and target changes.</acceptance_criteria>
  <done>Texture commands contain every parameter needed for deterministic history and later renderer evaluation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| UI numeric/preset input → domain | Untrusted values and texture references become durable commands. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-09 | Tampering | target/role command | high | mitigate | Domain guards fixed roles, revision, hidden/locked state and Foreground-only coverage. |
| T-01-10 | Denial of service | brush bounds | medium | mitigate | Validate diameter/spacing/jitter and checked geometry before allocation. |
| T-01-11 | Repudiation | mutable preset reference | medium | mitigate | Store resolved recipe, seed and algorithm identity with command. |
</threat_model>
<verification>Run contracts after each task and the connected Tracer case after the role migration; compare saved/reopened stroke identity.</verification>
<success_criteria>Fixed roles, guarded targets and deterministic texture commands are effective through the existing EditSession.</success_criteria>
## Artifacts this phase produces
- Fixed TerrainRole identities and commands for terrain state.
- ResolvedTextureBrush/texture-stroke records and stable parameter-version fields.
- BrushContracts cases for target guards, preset families and deterministic replay.
<output>Create .planning/phases/01-connected-imported-terrain/01-03-SUMMARY.md when done.</output>
