---
phase: 01.1-normalize-document-geometry-to-1-000-map-units-with-independ
plan: 08
type: execute
wave: 6
depends_on: ["01.1-07"]
files_modified:
  - Scripts/Rendering/GpuTerrainTracer.cs
  - Shaders/gpu_terrain_tracer.glsl
  - Scripts/Acceptance/GpuTerrainTracerCase.cs
  - Scripts/App/Main.cs
  - Scripts/run-connected-phase1.ps1
autonomous: true
requirements: [DOC-01, REND-04, REND-05]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "A strict trace starts its clock before derived source load/upload and reconstructs the whole declared visible set from immutable source plus all acknowledged strokes."
    - "No previous output texture, checkpoint or CPU-rendered changed frame supplies pixels to a strict generation."
    - "Every input has a matching durable revision, GPU generation, _Draw and FramePostDraw or is counted as a failure/stall; no latency is hidden by cache-state relabeling."
  artifacts:
    - Scripts/Acceptance/GpuTerrainTracerCase.cs
    - Scripts/Rendering/GpuTerrainTracer.cs
---

<objective>
Re-enter the halted R1 feasibility gate only far enough to measure the true legacy flattened reconstruction floor. Plan 07's first-generation 55.806/57.825/68.689 ms misses and off-clock preparation remain immutable counterexamples. Do not advance R2–R5 or promote Phase 01.1 from a warm or partial proof.
</objective>

<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-RENDERER-REDESIGN.md
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-07-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Implement a no-reuse full-visible-set GPU replay probe</name>
  <files>Scripts/Rendering/GpuTerrainTracer.cs, Shaders/gpu_terrain_tracer.glsl, Scripts/Acceptance/GpuTerrainTracerCase.cs, Scripts/App/Main.cs, Scripts/run-connected-phase1.ps1</files>
  <action>Add an opt-in pinned-Vulkan connected case for 15 durable 60-sample legacy edits. For each strict input, begin the monotonic clock before reading/validating the immutable prepared source file; discard derived CPU/GPU pages, prior output as a compute input, optional checkpoints and history acceleration; then upload source and replay all current legacy strokes on the main GPU device to a new display generation. Kernel infrastructure may remain only if its lifetime is separately enumerated and first-use cost is reported. The previous front texture may remain only as a display lease, counted against memory, never sampled or copied. Verify final and sentinel intermediate pixels against the independent CPU reference outside the timed path. Refuse a hidden full-frame CPU render/upload as implementation. Retain existing fast-path evidence separately.</action>
  <verify><automated>./Scripts/run-connected-phase1.ps1 -Case GpuTerrainStrict</automated></verify>
</task>
<task type="auto">
  <name>Measure the strict path without changing the acceptance denominator</name>
  <files>Scripts/Acceptance/GpuTerrainTracerCase.cs, Scripts/Rendering/GpuTerrainTracer.cs</files>
  <action>Run ≥60 seconds and ≥15 individually correlated strict edits at the inherited 1920×1080 window and actual 827×896 display raster. Preserve every sequence and cold source/read/upload, commit, dispatch, draw and frame interval. Report p50/p95/p99 and memory, explicitly count failed, missing, stalled and over-limit sequences. Keep the 50/100 ms total and 20/33.3 ms frame gates. The focused case exits nonzero if any required gate fails. Preserve raw evidence under a new unique trial path, never replace Plan 07 trials. If a complete strict replay is infeasible within this slice, record the exact technical blocker and halt rather than claim a verdict from a synthetic or warm substitute.</action>
  <verify><automated>./Scripts/run-connected-phase1.ps1 -Case GpuTerrainStrict</automated></verify>
</task>
</tasks>

<verification>A true strict reconstruction run is measured or an exact blocker is recorded. The GPU production switch remains off, Plan 05 remains failed, and modern/precision/export gates remain unverified.</verification>
<success_criteria>Complete, source-bound strict legacy evidence has an explicit physical pass/fail result without reuse of a prior generation.</success_criteria>
<output>Create 01.1-08-SUMMARY.md with exact raw paths and a truthful gate status.</output>
