---
phase: 01.1-normalize-document-geometry-to-1-000-map-units-with-independ
plan: 02
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - src/Mapwright.Infrastructure/LegacyMapUnitMigration.cs
  - src/Mapwright.Infrastructure/SqliteProjectRepository.cs
  - tests/Mapwright.ContractTests/LegacyMapUnitMigrationContracts.cs
  - Scripts/App/ConnectedUiSmoke.cs
  - Scripts/Core/ProjectStore.cs
autonomous: true
requirements: [DOC-01]
estimate: {tokens: 39000, raw_tokens: 39000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-06: a pre-amendment format-2 SQLite project is identified before normalized validation; no pixel coordinate is silently read as a map unit."
    - "Migratable baseline, current cursor and retained redo commands reopen in normalized space with the same apparent positions, sizes, IDs, order and source hashes; the original project remains recoverable."
    - "Unsupported/corrupt/disk-full legacy data returns an actionable refusal and leaves original SQLite, blobs, cursor and redo branch byte-for-byte intact."
  artifacts:
    - src/Mapwright.Infrastructure/LegacyMapUnitMigration.cs
    - src/Mapwright.Infrastructure/SqliteProjectRepository.cs
    - tests/Mapwright.ContractTests/LegacyMapUnitMigrationContracts.cs
  key_links:
    - "Schema version detection -> bounded staged copy -> all authoritative snapshots/commands -> replay cursor/latest -> published normalized sibling."
    - "Recent/open project UI -> version-aware repository result -> chosen normalized copy or explicit original-preserving refusal."
---

<objective>
Preserve Phase 1 project data and edit history when opening it under D-06, with a non-destructive normalized copy and an explicit safe refusal for payloads that cannot be converted exactly.

Purpose: Existing maps must keep their visible placement and redo state without corrupting the source project.
Output: version-aware legacy converter, SQLite copy migration, user-visible open result, durable fixture contracts.
</objective>

<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-CONTEXT.md
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-VERIFICATION.md
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-01-SUMMARY.md
</context>

<assumptions_and_boundaries>
Migration policy under agent discretion: create a sibling normalized project from a complete SQLite backup plus immutable blobs in a private staging directory, validate it, then publish the sibling atomically and open that path. The format-2 source remains untouched and listed as the recovery original. Re-running after interruption is idempotent and does not overwrite an existing normalized sibling. Unknown retained command types, unsupported prototype manifest projects, invalid old dimensions, subunit converted values that cannot be represented without visual change, and any ambiguous authority/cursor ordering cause an explicit refusal. Empty retained command history is valid when the baseline and cursor are valid; absent or inconsistent required baseline is not. Sequence order, equal-time ties, role order and redo-after-cursor order are preserved exactly.
</assumptions_and_boundaries>

<prohibitions>
Do not rewrite the original format-2 scene.sqlite or manifest, clamp converted geometry, drop an unknown command, rewrite source blob bytes/hashes, replace a valid destination, or treat history checkpoints/tile caches as authority. Do not claim a copied project is safe before replay and blob validation succeed.
</prohibitions>

<artifacts_this_phase_produces>
Normalized sibling project with converted authoritative revisions and commands; preserved original legacy project; explicit compatibility result and migration/refusal tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Open a retained format-2 SQLite history as a validated normalized sibling</name>
  <reversibility rating="costly">D-06 conversion changes a durable format and every retained command, but publishing a new sibling while preserving the original makes rollback an explicit path choice without rewriting the original.</reversibility>
  <files>src/Mapwright.Infrastructure/LegacyMapUnitMigration.cs, src/Mapwright.Infrastructure/SqliteProjectRepository.cs, tests/Mapwright.ContractTests/LegacyMapUnitMigrationContracts.cs</files>
  <read_first>01.1-CONTEXT.md D-01/D-05/D-06/D-07; SqliteProjectRepository.cs CreateSchemaAsync, LoadAsync, ReconstructRevisionAsync, ReadCommandsAsync, ValidateLoadedProjectAsync and command deserializer; HistoryContracts.cs cursor/redo and cache cases; MapModel.cs geometry-bearing stroke/Land/river/source records; 01.1-01-SUMMARY.md for normalized model fields.</read_first>
  <behavior>First write a genuine format-2 SQLite fixture containing baseline, ordered mixed texture/Land/river edits, cursor below latest and a retained redo branch. Assert before/after normalized map-point and diameter/width ratios, source hashes, command IDs/sequence/revision, exact cursor/latest replay, Undo/Redo after restart and cache deletion. Inject unknown command, invalid dimension, subunit unsupported value, interrupted staging and disk-full/copy failure; each must leave the source intact and produce a typed refusal.</behavior>
  <action>Per D-06/D-07, detect schema_info and snapshot format before normalized deserialization or validation. Copy the SQLite authority with SQLite backup semantics into a bounded sibling staging project, copy or verified-link immutable blobs without mutating them, and reject directory/source overlap. Convert every authoritative revisions.snapshot_json and commands.payload_json, including baseline, current cursor, latest redo and all retained command families; include legacy/resolved texture samples and anchors, Land samples/radii, river points/profiles, imported raster transforms, and stored bounds/effect distances. Scale geometric lengths and points by the same 1000/max(old width,old height), but assign the longer new project axis exactly 1000 through the shared factory; preserve dimensionless opacity, softness, spacing ratio, rotation, IDs, revision and sequence. Recompute derived bounds where appropriate; clear only derived history/tile acceleration and bump compatibility identity. Validate SQLite integrity, blob hashes, schema/snapshot agreement, full replay at cursor and latest, and visual geometry ratios before atomic sibling publication. Return a typed refusal for any converted subunit value below normalized validators; do not clamp or publish a sibling, and leave the format-2 original untouched. Test an empty valid command log and source-order ties explicitly.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>Any baseline/current/redo replay differs, a source byte/hash changes, an unsupported payload is accepted, a valid empty history is rejected, or a failure leaves a published partial sibling.</fails_when></verify>
  <acceptance_criteria>Original format-2 project is unchanged; normalized sibling has a 1000-unit longest edge, equivalent source-relative command geometry, stable IDs and exact cursor/latest; future Undo/Redo and cache-independent reopen work; unknown, invalid and converted subunit histories receive a typed actionable refusal with no sibling published.</acceptance_criteria>
  <done>Legacy SQLite data can be safely converted into a separate verified normalized project or refused without source mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Expose the legacy open outcome and reject unsupported manifest projects safely</name>
  <files>Scripts/App/ConnectedUiSmoke.cs, Scripts/Core/ProjectStore.cs, tests/Mapwright.ContractTests/LegacyMapUnitMigrationContracts.cs</files>
  <read_first>ConnectedUiSmoke.cs RecentProjectCatalog, OpenProjectPathCoreAsync and BindOpenedProject; ProjectStore.cs OpenProject and manifest path; LegacyMapUnitMigrationContracts.cs fixture from Task 1.</read_first>
  <behavior>Opening a format-2 recent folder yields the verified normalized sibling and retains a visible original path; opening an unsupported manifest or malformed SQLite yields a specific recovery instruction and no edit session; reopening the normalized sibling preserves cursor and redo; an absent folder is not created.</behavior>
  <action>Per D-06, route explicit Recent/Open project selection through version-aware detection before EditSession or canvas binding. On a verified migration result, remember and open the sibling while showing the retained original path and a clear saved/recovered revision. On unsupported prototype manifest or conversion refusal, show the precise reason and a safe action (keep/open the original with a compatible build or retry after repairing source), leaving the entry selectable and the working session unchanged. Keep the existing explicit-user-choice entry behavior and durable Save state. Add tests for manifest, missing folder, refused SQLite and successful normalized reopen; do not add a new migration settings screen.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>The UI treats an old manifest as normalized, binds a failed migration, loses the original path, or changes the active session after refusal.</fails_when></verify>
  <acceptance_criteria>Open reports either verified normalized sibling with source path or actionable unsupported-legacy status; source project and active editor state survive every refusal.</acceptance_criteria>
  <done>Legacy handling is reachable from the existing project-entry flow and never silently reshapes data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Legacy SQLite/manifest and project folder → migrator | Local files can contain corrupt or unsupported data and paths. |
| Staged sibling → authoritative project list | A partial or incorrectly replayed migration must not become user-visible authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01.1-04 | Tampering | legacy command replay | high | mitigate | Convert all retained payloads, compare sequence/cursor/latest replay and hashes, refuse unknown data before publication. |
| T-01.1-05 | Denial of service | staged copy/migration | high | mitigate | Checked lengths, bounded copy, cancellation and disk-full cleanup; original directory stays untouched. |
| T-01.1-06 | Repudiation | migration outcome | medium | mitigate | Return explicit source/sibling paths, schema versions, refusal reason and verified revision; retain original evidence. |
| T-01.1-07 | Elevation of privilege | project path traversal | medium | mitigate | Canonicalize source, staging and destination; reject overlap and symlink escape before copy/publication. |
</threat_model>

<verification>Run the contract registry, then reopen the real Phase 1 format-2 fixture through the existing Recent/Open flow. Validate cursor and redo after a fresh process; do not update the original Phase 1 acceptance report.</verification>
<success_criteria>D-06 legacy history and refusals are proved; no original project or immutable blob is destructively changed.</success_criteria>
<output>Create .planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-02-SUMMARY.md when done.</output>
