---
phase: 01.1-normalize-document-geometry-to-1-000-map-units-with-independ
plan: 03
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - Scripts/App/MapCanvas.cs
  - Scripts/App/EditorControls.cs
  - Scripts/App/GestureController.cs
  - Scripts/Acceptance/MapUnitGesturesCase.cs
  - tests/Mapwright.ContractTests/BrushContracts.cs
  - tests/Mapwright.ContractTests/MaskRiverContracts.cs
autonomous: true
requirements: [DOC-01]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-05/D-10: the same 96-map-unit brush gesture commits the same map-space center, diameter and bounds at different zoom and editing pixel counts."
    - "Land diameter, texture stroke position/radius/anchor and river points/width remain map-unit values; screen handles/markers remain fixed in screen pixels."
    - "Existing target, lock, hidden-layer, revision and one-gesture-one-command guards still work."
  artifacts:
    - Scripts/App/MapCanvas.cs
    - Scripts/App/EditorControls.cs
    - Scripts/Acceptance/MapUnitGesturesCase.cs
  key_links:
    - "Viewport pixel + pan/zoom -> MapCanvas map coordinate -> GestureController samples -> durable command."
    - "EditorControls numeric values in map units -> scope cue in screen pixels -> renderer output at selected sampling."
---

<objective>
Make editing gestures and their UI readouts use stable map-unit sizes while preserving screen-space cue readability and Phase 1 gesture guards.

Purpose: Changing zoom or editing resolution must not resize native brush/Land/river geometry.
Output: map-unit editor controls, invariant gesture conversion, connected and domain regression cases.
</objective>

<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-CONTEXT.md
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-PATTERNS.md
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-01-SUMMARY.md
</context>

<assumptions_and_boundaries>
Existing numeric brush/river limits become map-unit limits for newly normalized projects; legacy values are converted or explicitly refused by Plan 02, never clamped. A single-point gesture remains valid for texture/Land, whereas River requires its existing two-point rule. Adjacent gesture samples are deduplicated by a distance measured in map units, with first/last samples retained; this must not depend on zoom or editing pixels. Existing screen cue stroke/handle widths remain pixel constants even when the cue footprint is derived from map diameter.
</assumptions_and_boundaries>

<prohibitions>
Do not multiply committed diameters or river widths by editing/export pixels, store Godot Vector2 as document authority, add stamp/path/label controls, or change Phase 4 Grid and preset-selector UI. Do not weaken hidden/locked/role/revision guards.
</prohibitions>

<artifacts_this_phase_produces>Native map-unit gesture commands, correctly labeled map-unit controls and zoom-independent screen cues, plus D-05/D-10 connected evidence.</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Commit identical brush and Land geometry across zoom and editing sampling</name>
  <reversibility rating="costly">D-05 changes the semantic unit of existing edit controls and command payloads; undo requires coordinated UI/domain/persistence changes, while legacy data stays protected by Plan 02.</reversibility>
  <files>Scripts/App/MapCanvas.cs, Scripts/App/EditorControls.cs, Scripts/Acceptance/MapUnitGesturesCase.cs, tests/Mapwright.ContractTests/BrushContracts.cs</files>
  <read_first>01.1-CONTEXT.md D-04/D-05/D-10; MapCanvas.cs CanvasToDocument, BuildGestureCommand and DrawEditingScopeCue; EditorControls.cs NumericFieldState and ScopeCueContract; BrushContracts.cs resolved brush tests; Main.cs connected-case registry convention.</read_first>
  <behavior>A new connected case compares the same normalized pointer path at two zooms and 1K/4K editing samplings, asserting identical committed samples, 96-unit diameter, stable map-space bounds and same relative rendered footprint. It checks cue footprint conversion in X/Y, while crosshair/handles remain fixed screen-pixel sizes. Invalid diameter and hidden/locked targets still reject.</behavior>
  <action>Per D-04/D-05/D-10, use the normalized project bounds and persisted editing raster transform for pointer-to-map and map-to-screen calculations. Keep all committed texture/Land samples, radius and anchors in double map coordinates; label size fields and scope readouts as map units rather than pixels. Make the cue footprint reflect map diameter on both axes, including a rounded shorter raster edge, while retaining explicit screen-pixel line/marker widths. Register a new nonzero-assertion connected case in its own provider file and add domain assertions before adjusting production behavior. Preserve target/revision checks and one acknowledged command per gesture.</action>
  <verify><automated>./Scripts/run-connected-phase1.ps1 -Case MapUnitGestures</automated><fails_when>The case is undiscoverable/zero-assertion, committed coordinates or diameter differ by zoom/sampling, the Y footprint distorts, or a locked target accepts the gesture.</fails_when></verify>
  <acceptance_criteria>96 map units remains 96 in saved texture/Land commands at 1K and 4K editing; centers and relative bounds match to double tolerance; UI says map units and screen markers retain fixed pixel thickness.</acceptance_criteria>
  <done>Users can paint with stable map-unit dimensions independent of viewport zoom or edit sampling.</done>
</task>

<task type="auto" tdd="true">
  <name>Keep gesture sampling and river width in the same map coordinate contract</name>
  <files>Scripts/App/GestureController.cs, Scripts/Acceptance/MapUnitGesturesCase.cs, tests/Mapwright.ContractTests/MaskRiverContracts.cs</files>
  <read_first>GestureController.cs BeginEdit/AddSample/PrepareCommit and MinimumSampleDistance; MapCanvas.cs BuildRiverCommand; MaskRiverContracts.cs coverage, width and output-pixel-center cases; 01.1-CONTEXT.md D-05/D-07/D-10.</read_first>
  <behavior>At zoom 0.5, 1 and 4 the same physical map path yields the same retained map samples, first/last positions and one command; a river's per-point widths and bank softness remain unchanged. Adjacent sample dedup is measured in map units and does not reorder samples. Cancellation commits nothing.</behavior>
  <action>Per D-05/D-07/D-10, audit and correct GestureController's sample distance threshold, overflow last-point replacement and river creation/edit flow so each consumes map-unit points from MapCanvas without another pixel conversion. Preserve ordered input samples, stable river identity, Foreground-only coverage target, width interpolation and post-Land river subtraction. Add contract assertions for MapPoint positions, 96-unit brush/Land/river support and cancellation/redo-compatible command identity; extend MapUnitGestures to cover river interaction without changing the Phase 1 one-command gesture lifecycle.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>Zoom/edit sampling changes retained map samples or width, adjacency dedup reorders points, cancelled gestures persist, or existing mask/river semantics regress.</fails_when></verify>
  <acceptance_criteria>Equivalent gesture paths produce equal saved map-unit points and dimensions; river point widths and softness are stable; Land Add/Subtract and river ordering still pass contracts.</acceptance_criteria>
  <done>All current editing tools commit stable map geometry, with viewport pixel behavior limited to presentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Pointer/UI numeric input → immutable edit command | Zoomed screen values and user-entered dimensions become durable geometry. |
| Gesture plan → EditSession | Target/revision guards gate the authoritative command. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01.1-08 | Tampering | MapCanvas gesture conversion | medium | mitigate | Validate finite normalized points and allowed map-unit sizes before commit; test zoom/sampling equivalence. |
| T-01.1-09 | Elevation of privilege | terrain target guards | high | mitigate | Preserve Foreground-only mask/river, hidden/locked target and stale revision refusal below UI. |
| T-01.1-10 | Denial of service | GestureController samples | medium | mitigate | Retain MaximumSamples cap and ordered last-point replacement; test dense adjacent input. |
</threat_model>

<verification>Run MapUnitGestures, Gestures, ToolPanels and the contract registry. Compare committed map-space bounds rather than only screenshots.</verification>
<success_criteria>D-05/D-10 have saved-command and connected evidence at zoom and editing resolutions, with D-07 editing guards intact.</success_criteria>
<output>Create .planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-03-SUMMARY.md when done.</output>
