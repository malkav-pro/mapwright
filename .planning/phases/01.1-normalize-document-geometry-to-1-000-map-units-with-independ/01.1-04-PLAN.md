---
phase: 01.1-normalize-document-geometry-to-1-000-map-units-with-independ
plan: 04
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - Scripts/Rendering/ConnectedTerrainGraph.cs
  - Scripts/Export/DocumentPngExport.cs
  - tests/Mapwright.ContractTests/RenderContracts.cs
  - tests/Mapwright.ContractTests/ExportRecoveryContracts.cs
autonomous: true
requirements: [DOC-01]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-04/D-10: the same frozen map revision is evaluated at independent 1K and 4K export samplings; a 96-map-unit brush has equal relative bounds at both."
    - "D-03/D-09: imported raster bases retain their explicit placement/clipping and aspect across viewport and export; editable geometry is sampled at target pixel centers."
    - "D-07: 16K per-axis admission, bounded source/export memory, seams, cancellation and atomic destination preservation remain enforced."
  artifacts:
    - Scripts/Rendering/ConnectedTerrainGraph.cs
    - Scripts/Export/DocumentPngExport.cs
    - tests/Mapwright.ContractTests/RenderContracts.cs
    - tests/Mapwright.ContractTests/ExportRecoveryContracts.cs
  key_links:
    - "MapProject geometry + raster transform -> tile-local source sample and editable evaluation -> viewport/export matching pixel centers."
    - "Frozen revision + pinned blobs -> bounded target-resolution tiles -> PNG validation -> atomic publication."
---

<objective>
Sample one normalized map at independent viewport/export resolutions while retaining bounded 16K PNG publication and imported base alignment.

Purpose: Export pixels must be a sampling choice and never redefine or merely enlarge editable map geometry.
Output: transform-aware tile renderer, target-resolution frozen export, multi-resolution and resource regression contracts.
</objective>

<execution_context>
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/almar/Documents/Codex/mapwright/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-CONTEXT.md
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-RESEARCH.md
@.planning/phases/01-connected-imported-terrain/01-VERIFICATION.md
@.planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-01-SUMMARY.md
</context>

<assumptions_and_boundaries>
The public map export request accepts only 1024, 2048, 3072, 4096, 8192 or 16384 on the longest pixel edge, with the shorter edge derived once from normalized map aspect; the lower-level PNG writer may retain tiny arbitrary diagnostic dimensions for existing test fixtures. A raster outside its stored transformed footprint contributes transparent coverage rather than stretched edge pixels. Adjacent tile interiors share identical global pixel-center coordinates independent of tile order/halo; no empty tile invents source colour. Source raster pixels can only resample, while native strokes re-evaluate at target scale.
</assumptions_and_boundaries>

<prohibitions>
Do not allocate decoded input arrays proportional to a 16K output canvas, derive map bounds from PNG dimensions, nearest-neighbor enlarge a viewport frame as the only editable export source, emit nonuniform public output, or publish a partial/cancelled PNG. Do not change Phase 4 export selectors or Grid UI.
</prohibitions>

<artifacts_this_phase_produces>Transform-aware bounded tile evaluation and target-resolution frozen PNG rendering for all supported output counts.</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Evaluate transformed source rasters and native edits in bounded target tiles</name>
  <files>Scripts/Rendering/ConnectedTerrainGraph.cs, tests/Mapwright.ContractTests/RenderContracts.cs</files>
  <read_first>01.1-CONTEXT.md D-03/D-04/D-07/D-09/D-10; ConnectedTerrainGraph.cs GetInputs, EvaluateRegionAtSize, RenderReference, tile keys and input cache; Geometry.cs DocumentRasterTransform; RenderContracts.cs FrozenRevisionAndTiledAnchoring and DirtyTilesMatchMixedReference.</read_first>
  <behavior>Tests use a non-square source with offset/partial-extent raster plus 96-unit texture/Land/river geometry. Whole-frame and differently ordered adjacent tiles match within one colour channel at 1K/4K, the map-space stroke bounds are identical, gaps remain transparent, and a 16K tile request is admitted without a full-output decoded source cache.</behavior>
  <action>Per D-03/D-04/D-07/D-09/D-10, replace output-canvas-sized decoded input caches with bounded source-native or tile/strip-scoped sampling protected by RenderResourceLedger; retain immutable source identity and pin discipline. Inverse-map global output pixel centers through the shared DocumentRasterTransform into map coordinates, then through each stored raster transform to intrinsic source pixels, clipping outside the raster footprint. Evaluate texture, Land and river in the same global map coordinates, preserving Land-before-river order and current linear-premultiplied composition. Keep deterministic center/tie behavior, seam halos and conservative invalidation; bump renderer/derived cache compatibility so old pixel-coordinate tiles cannot be reused. Write the multi-resolution and resource tests before changing the graph.</action>
  <verify><automated>./Scripts/run-contract-tests.ps1</automated><fails_when>A source offset is lost, an empty/partial tile is painted, tile order or seam changes a pixel beyond tolerance, or a 16K tile causes full-output source allocation.</fails_when></verify>
  <acceptance_criteria>At 1K and 4K, corresponding normalized positions/brush bounds and imported raster edges align; 16K evaluation stays within the ledger; renderer cache keys cannot reuse prior coordinate semantics.</acceptance_criteria>
  <done>The graph samples source rasters and editable commands in stable map coordinates at requested target pixels.</done>
</task>

<task type="auto" tdd="true">
  <name>Export the frozen revision at selected output pixels with bounded atomic publication</name>
  <files>Scripts/Export/DocumentPngExport.cs, tests/Mapwright.ContractTests/ExportRecoveryContracts.cs, Scripts/Rendering/ConnectedTerrainGraph.cs</files>
  <read_first>01.1-CONTEXT.md D-04/D-07/D-10; DocumentPngExport.cs ExportAsync, CreateGraphCapture and GraphFrozenRenderer; ConnectedTerrainGraph.cs EvaluateRegionAtSize and ExportPng; ExportRecoveryContracts.cs publication/cancellation cases.</read_first>
  <behavior>Frozen graph export at 1K/4K matches graph reference samples and gives 96-unit stroke the same relative bounds; 16K is allowed but one pixel beyond per-axis cap is rejected; nonuniform public requests are rejected; cancellation, validation failure and disk error preserve an existing destination and clean partial output.</behavior>
  <action>Per D-04/D-07/D-10, replace graph capture's preview-frame scaling with a frozen map snapshot and pinned source leases whose RenderAsync asks ConnectedTerrainGraph to evaluate each requested output tile at the full output dimensions and global offsets. Keep the existing bounded band height, halos, checked byte ledger, progress/revision identity, PNG CRC/sRGB/straight-alpha validation and atomic replace. Provide a map-facing longest-edge preset request path that derives the shorter edge with the shared rounding policy and rejects nonuniform/custom requests; retain the low-level tiny-dimension writer only for diagnostics. Verify that later edits cannot affect a captured export and that original destination bytes survive all prepublication exits. Keep 16K per-axis limit, not a smaller editing-resolution cap.</action>
  <verify><automated>./Scripts/run-connected-phase1.ps1 -Case ExportPublication</automated><fails_when>The frozen output is derived only from preview pixels, exceeds memory envelope, accepts unsupported public dimensions, mutates after later edits, or replaces the destination on cancellation/failure.</fails_when></verify>
  <acceptance_criteria>1K/4K exports evaluate native edits at target centers, output aspect follows 1000-unit geometry, 16K stays bounded, and publication remains atomic and cancellable with Phase 1 colour/alpha guarantees.</acceptance_criteria>
  <done>One immutable map revision exports at supported independent pixel resolutions without quality or safety regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Stored source raster/transform → renderer | Malformed coordinates can trigger incorrect reads or excessive allocation. |
| Export request/path → filesystem publication | User output dimensions/path and cancellation control durable destination bytes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01.1-11 | Denial of service | 16K renderer/export | high | mitigate | Checked source/tile/band arithmetic and RenderResourceLedger admission before allocations. |
| T-01.1-12 | Tampering | frozen export snapshot | high | mitigate | Pin immutable blobs and exact project ID/revision; target tiles evaluate that snapshot only. |
| T-01.1-13 | Tampering | PNG destination | high | mitigate | Keep path containment/recheck, partial file, full validation and atomic replace; test cancellation/error preservation. |
| T-01.1-14 | Information disclosure | source raster sampling | low | accept | Local raster bytes stay inside the user's project/export boundary; no new remote sink is introduced. |
</threat_model>

<verification>Run contracts, RenderReference and ExportPublication. The full real-hardware 16K/resource matrix belongs to Plan 05 and must retain measured byte evidence.</verification>
<success_criteria>D-04/D-07/D-09/D-10 render/export invariants pass at independent samplings with bounded 16K output.</success_criteria>
<output>Create .planning/phases/01.1-normalize-document-geometry-to-1-000-map-units-with-independ/01.1-04-SUMMARY.md when done.</output>
