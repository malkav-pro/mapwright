---
phase: 01.1-normalize-document-geometry-to-1-000-map-units-with-independ
plan: 06
subsystem: acceptance-evidence
tags: [powershell, godot, evidence, gpu-redesign]
requires:
  - phase: 01.1-05
    provides: Connected cases and the failed physical acceptance baseline
provides:
  - Unique leased Phase 01.1 run roots for connected artifacts
  - Run manifest binding fixture, code, shaders, captures, logs and workload definitions
  - Negative controls for stale/escaped roots and tampered evidence
affects: [01.1-07, renderer-redesign, acceptance]
tech-stack:
  added: []
  patterns: [run-scoped artifact lease, complete file-hash manifest, fail-closed visual review]
key-files:
  created: []
  modified:
    - Scripts/run-phase1-acceptance.ps1
    - Scripts/run-phase11-acceptance.ps1
    - Scripts/App/ConnectedUiSmoke.cs
    - Scripts/Acceptance/MapUnitsConnectedCase.cs
    - tests/Mapwright.ContractTests/AcceptanceContracts.cs
key-decisions:
  - Fresh Phase 01.1 evidence is written under artifacts/phase11-acceptance/runs/<run-id>; older run roots and the historical Phase 1 paths are not copied or overwritten.
  - Visual review is a separate per-run record under artifacts/phase11-acceptance/reviews/<run-id>.json and is absent until explicitly performed.
  - The inherited edit-to-visible clock is preserved; whether every in-progress pointer sample must meet 50 ms remains an unresolved product-contract question.
requirements-completed: []
actuals:
  tokens: 9578
  tasks: 2
  commits: 1
plan_head_before: ad0c18b396bd26b25ceb3eabb5d7615b0be87a7d
duration: 30min
completed: 2026-09-23
status: complete
---

# Phase 01.1 Plan 06: Evidence and Workload Contract Summary

Phase 01.1 connected evidence now has an isolated run identity, with file-level provenance and fail-closed verification; the physical latency gate remains failed.

## Accomplishments

- Full runs allocate a fresh ID and an active lease before writing. The connected ScopeCues, EntryImportUi, Gestures, ShellStates and MapUnits cases resolve their outputs beneath that run. A Phase 1 run retains its original paths.
- The run manifest records SHA-256 and byte counts for code, GLSL shaders, fixture and all run artifacts, including captures and correlated hardware JSON. Verification rejects missing, changed or newly introduced files, stale review identity, a dispatch-only endpoint, duplicate or missing stage sequences, and dropped inputs.
- The workload contract separately records the inherited 1920×1080 window/896-pixel display limit, 1K/4K/16K rasters, inherited 96-unit radius and current 96-unit diameter. It names the current clock, pending pointer/GPU timepoints and warm/cold/inherited-evicted/strict-reconstruction states without inventing measurements.
- Five of the ten historical Phase 1 pinned captures currently differ from their original hashes. The new runner discloses this mismatch; it does not overwrite or rebaseline them.

## Verification

- `./Scripts/run-phase11-acceptance.ps1 -SelfTest`: **PASS**, 79/79 contract cases. The script also verified a clean manifest, then rejected a tampered artifact and shader record.
- A post-close runner SelfTest explicitly exercises the VerifyReport correlation guard: a complete visible sequence is accepted, while a dispatch-only endpoint and a missing stage sequence are both rejected. `AcceptanceContracts` separately rejects dropped input IDs.
- `git diff --check` on all five changed files: **PASS**.
- No new physical `-Full` run was performed. Its twelve 60-second hardware rows and reviewer captures remain unverified; Phase 01.1 Plan 05 and `01.1-ACCEPTANCE.md` remain failed.
- The Main.cs ImportBounds and Tracer artifact-root routing is being committed by the concurrent R1 owner. This plan's code commit does not contain Main.cs.

## Task Commits

- `40d8d0b` — `feat(01.1-06): isolate and bind acceptance evidence` (both closely coupled tasks; one explicit-file code commit).
- Post-close repair: `41a86d8` — `test(01.1-06): reject dispatch-only and missing-stage evidence`. The frontmatter `actuals.commits` was measured at the original summary write, before this requested follow-up and the independent R1 commit.

## Deviations from Plan

- The two tasks were implemented in one commit because the runner, manifest and verifier share the same file and must change together. The plan labels both tasks `tdd="true"`, but has no `<behavior>` blocks and the configured runtime TDD gate was inactive; no RED evidence or RED commit is claimed.
- Phase 01.1 `-ReportOnly` is disabled for saved runs because regenerating a report in a previous run would mutate evidence. `-VerifyReport -RunId <id>` is read-only.
- The fresh visual review location is per-run. The historical `Scripts/phase11-reviewed-visuals.json` cannot approve a new run.

## Known Limitations

- The first new Full run will be unreviewed until its ten captures are inspected and a matching per-run review record is created. This is an intentional gate, not an acceptance pass.
- GPU submit/complete and pointer-event/gesture timestamps are defined but not yet recorded by the inherited harness. R1 owns instrumentation and the feasibility measurement.
- R0 does not improve renderer latency or satisfy DOC-01/REND-04; their failed acceptance remains authoritative.

## Threat Flags

| Flag | File | Description |
|---|---|---|
| threat_flag: filesystem path | Scripts/App/ConnectedUiSmoke.cs | Run-root environment input is a file-write trust boundary; exact run path, lease, and reparse-point guards reject redirects. |

## Self-Check: PASSED

The plan and summary files exist, code commit `40d8d0b` is on this branch, and the persisted pre-plan ledger measures one production commit before metadata close-out.
